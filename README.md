# radiation-calc — RadCalc

[![DOI](https://zenodo.org/badge/DOI/10.5281/zenodo.22794265.svg)](https://doi.org/10.5281/zenodo.22794265)

`radiation-lab.com` 우산의 셋째 lab. **방사선 실무 계산기** 묶음이고 `/calc/` 에 산다.

★ 위 배지는 **개념 DOI** 다 — **항상 최신 판본**을 가리키므로 릴리스를 내도 낡지 않는다.
판본 DOI(릴리스마다 새로 발급)는 배지에 쓰지 않는다. 화면 정본은 `brand.ts` 의 `DOI`,
발급 절차와 밟은 함정은 `docs/zenodo.md` 4절.

우산 정본은 `aijhmin1031-eng/radiation-lab`, 절차는 `radiation-meter-tester/docs/umbrella-setup.md`.

## ★★ 새 세션은 여기부터 (2026-09-20 기준)

### 현재 상태

`main` = `ac719fb`. 라이브 `radiation-lab.com/calc/` · 공개 쪽 **167장** · `npm test` **143건 통과**.
도구 **7종 전부** 유효성 평가가 서 있고 낱장 8쪽이 공개돼 있다(`/validation/`), 7건 모두
스스로 「All checks pass」를 선언한다(케이스 id 266개 대조).

핵종 낱장 147장의 **되풀이 줄이기 ①~④ 가 끝났다**(배포·라이브 확인 완료):

| | 시작 | 지금 |
|---|---|---|
| 산문 되풀이 몫 중앙 | 66.4% | **27.9%** |
| 최악 | 80.6% | **38.8%**(cd-109) |
| 숫자 뺀 낱말 겹침 | 31.9% | **16.6%** |
| 제목 틀 겹침 | 75.0% | **20.0%** |
| 147장 전부에 같은 제목 | 4개 | **0개** |
| 5-그램 겹침(옛 ④) | 9.9% | **4.4%** |
| 낱장 최소 어수 | ~250 | **271** |

### 새 세션 시작 절차

1. **원격 브랜치를 먼저 전수로 본다** — `git ls-remote --heads origin`.
   `git branch -a` 는 클론에 받아 온 것만 보여 준다(자매 레포가 하루에 두 번 밟은 함정이다).
   ★ 이 레포는 **squash 병합**을 쓰므로 브랜치 커밋이 `main` 이력에 안 보이는 것이 정상이다.
   내용이 들어갔는지는 `git show origin/main:<파일>` 로 확인한다.
2. `npm ci && npm run build` — 게이트는 전부 `dist/` 를 읽는다. **빌드 없이 돌리면 헛돈다.**
3. `npm run typecheck` (= `tsc --noEmit` + `astro check`) · `npm test` (143건)
   ★ **`tsc` 는 `.astro` 를 한 글자도 보지 않는다** — 쪽의 타입은 `astro check` 만 본다.
   2026-09-20 까지 CI 에 그것이 없어 **오류 3013건이 서 있었다**(일지 그날 ①). 지금은 CI 에 있다.
4. `npm run gate` — 게이트 **12개**를 한 줄로 돌린다. ★ `check-render` 만 **4~5분** 걸리므로
   빠른 확인에는 그것을 빼고 돌리고, 커밋 전에 한 번 전수로 돌린다.
   ★ 크로미움 경로는 **`gate/chromium.mjs` 한 곳**이 정하고 미리 깔린 것을 스스로 찾는다 —
   **`npx playwright install` 을 돌리지 말 것**(원격 컨테이너가 금한다). 판이 어긋나
   「Executable doesn't exist」가 나면 `PW_CHROMIUM=<경로>` 로 덮는다.

### 게이트 한눈에 — ★ **각 게이트가 못 보는 것**이 중요하다

| 게이트 | 보는 것 | **못 보는 것** |
|---|---|---|
| `check-output` | 산출물 정적 검사 · **본문 어수 하한 250** | **되풀이로 채운 분량을 분량으로 센다**(2026-09-20 에 드러났다) |
| `check-render` | 전 쪽 × 2뷰포트 조판·JS 오류 실측 | 스크롤 0 에서만 잰다 · **간헐 404 미해결**(아래) |
| `check-nuclides` ④ | 낱장 사이 5-그램 겹침 | **`prose-doc` 안만** 본다 · **숫자를 남겨** 겹침이 씻긴다 |
| `check-nuclides` ④-2 | 산문 전체(`<p>` + 12어 이상 `<li>`) · 숫자 제외 · 제목 틀 | **조립된 영문이 말이 되는지** · 사실이 맞는지 · ★ **표·표 밖 껍데기를 안 본다** — 그래서 제목이 템플릿에 박혀 있던 것을 못 봤다(`check-adsense` 가 잡았다) |
| `check-validation` | 케이스 커버리지 · **엔진 임포트 금지** · 화면 실측 · **보고서 판정** | 케이스가 **정본과 맞는지**(사람이 손계산으로 세운다) |
| `check-og` | 구운 그림의 제목과 현재 제목 대조 | 그림이 **예쁜지** |
| `check-adsense` | **심사자의 시야로** 산출물 전체 — 광고 원천 0 · 밝히지 않은 바깥 원천 · 층별 분량 · 8어절 겹침 · **계열 안의 제목 뼈대**(이름을 지우고) · 법무 문서에 닿는가 · **색인 쪽 바닥**(빌드가 온전한가) | 글이 **사실인지** · 심사자가 실제로 어떻게 볼지 |
| `check-notes` | **읽을 글이 허브에서 찾아지는가** — 등록부(`src/lib/notes.ts`)의 쪽마다 허브 **본문**에 보이는 링크가 있는가(머리글·꼬리말은 **안 센다**) · **실제로 눌러** 도착지 확인 · 쪽이 서는가 · 두 뷰포트 · **양방향**(산출물에 있는데 **등록부에 없는** 쪽) | 글이 **좋은지**(분량·겹침은 `check-adsense`, 조판은 `check-render`) · **허브 밖의 링크** — 있어도 좋지만 **있다고 찾을 수 있는 것은 아니다**(그것이 이 게이트를 만든 결함의 정체다) |
| `check-gates` | **게이트 자신** — ① playwright 를 쓰는 **모든** 게이트가 크로미움 경로 정본을 쓰는가 ② ★★ **게이트가 두 자리에 다 적혔는가**(`.github/workflows/ci.yml` **와** `package.json` 의 `gate`) — CI 는 게이트를 **단계마다 따로** 부르므로 스크립트에만 넣으면 **CI 에서 안 돈다**(★ 수를 적지 않는다 — 게이트가 늘 때마다 낡는다) | 게이트가 **무엇을 재는지**(재는 대상은 각 게이트 몫) |
| `astro check`(CI) | `.astro` 프런트매터·템플릿 타입 | 산출물 · 값이 맞는지 (`tsc` 는 `.astro` 를 **아예 안 본다**) |

★★ **어떤 게이트도 「글이 말이 되는가」를 못 본다.** 2026-09-20 하루에 게이트를 전부 통과한 채
나간 결함이 **다섯**이었다 — 사실이 틀린 문장(Γ 순위 방향) · 본문과 어긋난 제목(Pu-239) ·
아무것도 안 알려 주는 문장(U-238 의 「100.00% … 100.00%」) · 눈금을 안 가리키는 띠 이름(H-3) ·
단위가 빠진 문장(「10.3 of lead」). **고친 쪽의 글을 소리 내어 읽어 볼 것.**

★★ **자를 새로 세우거나 고치면 역테스트를 반드시 붙인다** — 그리고 **건드리는 자리를 맞게 고를 것.**
되풀이 문단을 `prose-doc` **안**에 넣은 첫 역테스트는 아무것도 증명하지 못했다(④ 도 함께 움직여
사각이 안 보였다). **바깥**에 넣고서야 갈렸다.

### 미결 사항

1. **`check-render` 의 간헐 404 — 원인 미확정.** 라운드마다 다른 핵종에서 뜨고 **같은 빌드가
   ❌→✅→✅** 였다. `/favicon.ico` 가설은 **실측으로 기각**했다(60장 연속 적재에서 한 번도
   안 불렸다). 게이트가 이제 **주소와 상태를 이름으로 댄다** — 다음에 걸리면 거기서 시작한다.
   ★ 2026-09-20 에 같은 `dist/` 로 **전수 두 라운드를 연속으로 돌렸고 404 가 0건**이었다
     (한 라운드는 `CHROMIUM_PATH` 를 손으로 주고, 한 라운드는 정본의 자동 탐지로).
     **간헐성이 다시 확인된 것이고 원인은 여전히 미확정**이다 — 쫓아가지 말고 **덫을 걸어 둔
     채** 다음에 걸릴 때 그 메시지에서 시작하는 것이 싸다.
2. **수파베이스 가입자 수 — 소유주 몫.** 권한 분류기가 운영 읽기를 막고(「Production Reads」),
   에이전트가 자기 권한을 넣는 것도 막는다(「Self-Modification」). 대시보드 →
   Authentication → Users 가 가장 빠르다. **구조적으로 풀려면** `sql/` 에 집계 전용 함수를
   하나 두어 anon 키로 읽히게 한다 — 누구에게 열지(로그인 사용자만/비밀 인자/공개)만 정하면 된다.
3. **★ Supabase 개인 액세스 토큰 `RadiationLab_v3` 폐기 필요** — 2026-09-20 대화에서
   값이 화면에 노출됐다. 계정 전체 권한이라 프로젝트 셋 모두에 닿는다.
4. **산문 되풀이 27.9%** — 더 내리려면 광자선·알파 문단도 띠로 가른다(차폐·Γ·첫 문단은 끝냈다).
   ★ 띠를 나눌 때는 **어느 띠도 절반(74장)을 넘지 않아야** 한다. 구획을 눈대중으로 정하지 말 것.
5. **Search Console 색인 요청** — 유효성 평가 8쪽. 주소 하나씩 넣어야 한다(소유주 몫).

## ★★ 낱장 묶음 밖의 쪽 셋 — 색인의 단위를 잎에서 묶음으로 (2026-09-24)

핵종 낱장 147장은 **한 틀에서 나온다**(사이트 205주소의 72%). 심사자도 크롤러도 표본으로
몇 쪽을 여는데, 72%가 한 틀이면 무작위 표본은 거의 확실히 전부 그 틀이다 — 구글 스팸 정책의
**scaled content abuse** 가 겨냥하는 모양이다. 방어는 실재하지만(쪽마다 계산된 고유 숫자,
뼈대 겹침 0) **그건 우리만 안다.**

★★ **그래서 개수가 아니라 종류를 더한다.** 147 → 150 이 중요한 게 아니라, 표본을 열었을 때
**「이건 생성물이 아니다」가 분명한 쪽**이 섞이는 것이 요점이다. 셋 다 **고르고 비교하는
판단**이 들어가 낱장에서 파생시킬 수 없다.

| 쪽 | 본문 | 무엇이 생성 불가인가 |
|---|---|---|
| `/daughter-shielding/` | 869어 | 어느 딸이 평형에 드는지·누가 지배하는지가 핵종마다 다르다 |
| `/beta-shield-materials/` | 887어 | 저Z 먼저·고Z 뒤라는 **순서**는 값이 아니라 판단이다 |
| `/dataset-limits/` | 811어 | **한계를 스스로 밝히는 쪽**은 틀에서 나오지 않는다 |

★★ **이 줄이 2026-09-26 까지 거짓이었다.** 「허브·베타 도구·methods 에서 잇는다」고 적어
  두었는데 **허브에는 링크가 0개**였다 — 닿는 유일한 길이 도구 쪽 산문 속의 링크 하나였고,
  소유주가 화면에서 잡았다(「이건 랜딩페이지에서 어디로 어디로 들어와야 해? 찾기가 힘드네」).
  지금은 허브에 **읽을 글 구획**이 서 있고 정본은 `src/lib/notes.ts` 다(아래 절).
★ **숫자를 글자로 박지 않는다** — 셋 다 엔진·자료·`decay-chain.ts` 에서 파생된다.
  특히 ③은 **한계를 자료에서 센다**: 글자로 적으면 자료가 자랄 때 **한계에 대한 진술이 먼저
  낡는다.**

**만들며 잡은 것 넷**
· `href="/nuclides/…"` 를 맨으로 적어 base 가 안 붙었다 — 이 레포는 `u()` 로 감싼다.
  **빌드는 통과한다.** 산출물을 훑어 잡았다.
· ★★ **쪽이 스스로 모순이었다** — `Ir-192m2` 가 광자 절과 「분리 시점에 달린 경우」 절에
  동시에 들어가, 앞에서는 「a source of them **has** a photon field」라고 단정하고 뒤에서는
  「알 수 없다」고 했다. **조립된 문장을 읽어야만** 보였다(게이트는 전부 통과한 상태였다).
· ★★ **베타 표에 감마 선원이 섞였다** — Co-60 이 종점 1.49 MeV 로 상위에 드는데 실제
  선원에서는 광자장이 압도한다. **표에서 빼지 않고 밝혔다** — 종점은 맞는 값이고 빼면 다른
  거짓이 된다.
· 모바일에서 문서가 **252px 넘쳤다**(7열 표를 스크롤 컨테이너 없이 뒀다). 이미 있는
  `.pin-first` 규약에 넣고 좁은 화면에서 세 칸을 접었다.

## ★★★ 「고아가 아니다」와 「찾을 수 있다」는 다른 층이다 (2026-09-26)

소유주가 화면에서 잡았다 — 「**Shield materials for beta emitters** … 이건 랜딩페이지에서
어디로 어디로 들어와야 해? **찾기가 힘드네**」.

**실측한 동선** — 닿는 길이 **하나뿐**이었고 그것이 도구 쪽 **산문 속의 링크**였다.

| 쪽 | 들어오는 링크 | 허브(`/`) |
|---|---|---|
| `/beta-shield-materials/` | `/beta/` · `/daughter-shielding/` | **0** |
| `/daughter-shielding/` | `/nuclides/` | **0** |
| `/dataset-limits/` | `/methods/` | **0** |

우산 랜딩이 `/calc` 로 내보내는 링크 여섯에도 셋 다 없었다. 즉 **허브까지 온 사람이
그 도구를 열어 읽지 않으면 이 글들이 있다는 것조차 알 수 없었다.**

★★★ **내가 무엇을 잘못 쟀나.** 이 셋을 세울 때 「고아 쪽을 만들지 않는다 — 베타 도구·
methods·서로에서 잇는다」를 확인하고 통과시켰고 **그 말은 사실이었다**(들어오는 링크 ≥ 1).
그런데 그 검사는 **「허브에서 닿는가」를 묻지 않는다.** 앞엣것만 재고 뒤엣것을 잰 것처럼
넘어간 것이다. **위 108행이 「허브에서 잇는다」고 적고 있었는데 거짓이었다.**

★ **원인은 링크를 빠뜨린 것이 아니라 뼈대다.** 허브가 **도구 목록**이라(Unit converter ·
Decay · Gamma · Mass · MDA · Beta · ALARA + methods 산문) **읽을 글이 설 자리가 구조에
없었다.** 그래서 자리를 만든다 — 「Longer notes」 구획.

**정본은 `src/lib/notes.ts` 하나다.** 허브 구획과 게이트가 **같은 배열**을 읽는다.
★★ `TOOLS` 에 끼워 넣지 않았다 — 그 배열은 계산기의 정본이고 신뢰도 삼항(`assumes`·
`excludes`·`checked`)과 입력 요구(`needs`)를 든다. 읽을 글에는 그중 무엇도 뜻이 없고,
억지로 채우면 **도구 카드가 말하는 것이 무엇인지 흐려진다.** 구획을 가르는 것이 읽는
사람에게도 맞다 — **하나는 쓰는 것이고 하나는 읽는 것이다.**

**게이트 `npm run check-notes`**(`npm run gate` 에 포함). **묻는 것을 바꿨다** —
「들어오는 링크가 있는가」가 아니라 **「허브에서 몇 번에 닿는가」**다.
· 허브 **본문**에 보이는 링크가 있는가 — **머리글·꼬리말은 세지 않는다**(모든 쪽에 있어
  「허브에 있다」를 언제나 참으로 만든다 = 재지 않고 통과하는 검사가 된다)
· **실제로 눌러** 도착지를 확인한다(`u()` 를 빠뜨리면 base 밖으로 간다)
· 도착한 쪽에 `h1` 이 있고 본문이 서는가 · 두 뷰포트(390 · 1280)
· ★★ **양방향** — 등록부에 있는데 산출물에 없거나, **산출물에 있는데 등록부에 없거나.**
  **뒤엣것이 핵심이다**: 앞엣것만 보면 **새 글을 등록하지 않는 것**이 그대로 통과하고,
  그것이 이번에 일어난 일이다.

**역테스트 넷, 넷 다 잡혔다**
① 허브 구획을 통째로 삭제(=고치기 전 상태) → 6건 · ② `u()` 제거 → 6건 ·
③ **새 쪽을 만들고 등록부에 안 적음** → 「등록부에 없는 최상위 쪽 1개」 ·
④ 등록부가 없는 쪽을 가리킴 → 「쪽이 서지 않는다」

★ **못 보는 것을 적어 둔다** — 글이 좋은지(분량·겹침은 `check-adsense`, 조판은
`check-render`) · **허브 밖의 링크**(도구 산문·`/methods/` — 있어도 좋지만 **있다고 찾을 수
있는 것은 아니다**, 그것이 이 결함의 정체다) · 사람이 실제로 누르는지.

★ **우산 랜딩은 건드리지 않았다.** 랜딩의 역할은 lab 으로 보내는 것이지 낱쪽 목록이
아니다 — 거기에 읽을 글을 늘리면 lab 이 넷이 될 때 랜딩이 먼저 무너진다.

## ★★★ 게이트를 만들고 CI 에 안 붙이면 **내가 기억할 때만 도는 것**이다 (2026-09-26)

`check-notes` 를 세우고 `package.json` 의 `gate` 에만 등록한 채 PR 을 올렸다.
**이 워크플로는 게이트를 단계마다 따로 부른다** — `npm run gate` 를 쓰지 않는다.
그래서 새 게이트는 **CI 에서 안 돌았다.** 진행 중인 실행의 step 목록을 손으로 훑다가
없는 것을 보고서야 알았다. **자리가 둘인 것 자체가 함정**이고, 사람이 한쪽만 적는 것을
막을 방법은 **세는 것**뿐이다.

★ 그래서 `check-gates` 에 ②를 더했다 — `gate/check-*.mjs` 를 훑어 **워크플로와 `gate`
스크립트 양쪽에** 있는지 본다. 역테스트 둘(워크플로에서 지움 · 스크립트에서 지움) 다 잡혔다.

★★★ **그리고 그 자가 곧바로 둘을 더 찾았다 — 내가 만든 것이 아니다.**

| 게이트 | 상태 |
|---|---|
| `check-lastmod` | **CI 에 없었다.** 위 checkout 에 **그 게이트를 위해** `fetch-depth: 0` 을 걸어 두고 **정작 단계를 안 넣었다** — 비용은 내고 값은 못 받고 있었다 |
| `check-adsense` | **CI 에 없었다.** 애드센스 신청을 앞두고 가장 자주 읽던 게이트인데 **자동으로는 한 번도 안 돌았다** |

둘 다 이 커밋에서 붙였다. **지금 CI 의 게이트 단계는 16개**이고, 수는 여기 적지 않는다 —
`check-gates` 가 센다.

★ 교훈: **「게이트를 세웠다」와 「그 게이트가 돈다」는 다른 층이다.** 이 레포가
「고아가 아니다 ≠ 찾을 수 있다」를 배운 바로 그날 같은 모양을 한 겹 위에서 또 밟았다.

## ★★ 본문 삽화 — `figure.plate` 와 도면 서명란 (2026-09-24)

조판 규약은 **RadiMeter 에서 그대로 가져왔다**(`global.css` 의 `.plate`). 두 lab 이 같은
플랫폼에 사니 삽화 규칙이 갈리면 층마다 다른 말을 하게 된다. 다크에서 `brightness(.78)`.

★★★ **그림 속 글자 규칙을 실측으로 뒤집었다.** 옛 규칙은 「모델에게 맡기지 않는다」였고
근거는 **모델이 글자를 못 쓴다**는 것이었는데 **더는 사실이 아니다** — `ACRYLIC`·`BETA`·
`LEAD`·`X-RAYS` 를 도면 라벨로 뽑아 **원본 2배 확대로 읽었고 전부 정확했다.**
· **짧은 설명 라벨은 모델이 그려도 된다.** 다만 **뽑을 때마다 확대해 읽는다.**
· ★★★ **자료에서 나온 숫자는 여전히 그리지 않는다.** 이유가 철자가 아니라 **동기화**다 —
  그림 속 `15.0 mm` 는 자료가 바뀌어도 안 따라와 **조용히 낡고, 어떤 게이트도 래스터 안의
  숫자를 못 읽는다.** 수치는 `figcaption` 과 본문이 들고 그 둘은 자료에서 파생된다.

**서명란** — `scripts/sign-plate.mjs` 가 마크·이름·주소를 오른쪽 아래에 합성한다.
· 떠 있는 워터마크가 아니라 **도면 표제란**이다 — 연필 제도 언어 안에 있다.
· ★★ **마크를 흑연 한 색으로 쓴다.** 파비콘은 기준선이 앰버인데 판 안에서 앰버는 **뜻을
  가진 색**(정지점)이라, 서명에 두 번째 앰버가 생기면 읽는 사람이 거기서 뜻을 찾는다.
· ★ **다크에서 재고 넣는다** — 판 전체가 어두워지므로 옅으면 사라진다.
  실측: 라이트 **8.8:1** · 다크 **6.7:1**(둘 다 AA 4.5 초과).
· ★ 원본은 `art/plates/<이름>-raw.png` 에 둔다(`public/` 밖, 서비스 안 됨).
  **같은 프롬프트가 같은 그림을 주지 않으므로** 원본이 없으면 서명을 고칠 때마다 새로 뽑는다.
· ★ 첫 판은 버렸다 — 삼엽을 캡슐에 그렸고(위험 표지를 장식으로 쓰지 않는다), 두께 차이를
  4~5배로 그려 **쪽의 주장(697배)을 나르지 못했다.** 그림이 주장을 안 나르면 장식이다.
· ★ **게이트가 이 스크립트를 잡았다** — 크로미움 경로를 스스로 읽었다. 정본은
  `gate/chromium.mjs` 의 `LAUNCH` 다(경로를 아는 자리가 열한 곳이라 원격에서 게이트가
  멈춘 사고 뒤에 하나로 모은 것이다).

## ★★★ 막아야 할 것이 딸핵종이었다 — 낱장이 그것을 말하지 않았다 (2026-09-23)

색인이 35에서 안 움직이는 이유를 캐다 **정확성 결함**을 찾았다. 낱장 147장이 **딸핵종을
한 번도 말하지 않는다.** 그런데 여러 핵종에서 **차폐해야 할 것은 모핵종이 아니라 딸**이다.

라이브의 Ru-106 쪽: 「The beta endpoint is 0.0394 MeV … That endpoint stops in
**0.0215 mm of acrylic**.」 Ru-106 선원은 예외 없이 Rh-106(30초, 종점 3.54 MeV)과 평형이라
실제로는 **15.0 mm** 가 필요하다 — **697배**다.

| 모핵종 | 딸 | 쪽이 말하던 두께 | 실제 필요 | 배수 |
|---|---|---|---|---|
| Ru-106 | Rh-106 | 0.0215 mm | **15.0 mm** | **697×** |
| Pb-210 | Bi-210 | 0.0520 mm | 4.21 mm | 81× |
| Si-32 | P-32 | 0.446 mm | 6.70 mm | 15× |
| Sr-90 | Y-90 | 1.57 mm | 9.28 mm | 5.9× |
| Ce-144 | Pr-144 | 0.680 mm | 11.4 mm | 17× |
| Ge-68 | Ga-68 | (광자 기록 없음) | Γ **0.129** | — |

★★ **숫자는 하나도 틀리지 않았다. 틀린 것은 결론이다.** 이 레포가 되풀이해 적어 온
「빌드는 통과하는」 함정의 물리판이고, **면책 문구로 덮을 수 있는 종류가 아니다.**
★ 낱장에 「관련 핵종」 절이 **이미 있었는데** Sr-90 에 `Sr-85 · Sr-85m · Sr-89` 가 걸려 있었다 —
  **이름이 같은 것**이지 연쇄상 관계가 아니다. 물리적 관계를 빠뜨리고 명명 관계를 넣어 둔 셈이다.

**정본은 `src/lib/decay-chain.ts`** — 낱장과 게이트가 **같은 판단**을 읽는다.
· 딸핵종 신원은 `z`·`a`·`decay` 에서 나오는 **산술**이다(β⁻→Z+1 · β⁺/EC→Z−1 · α→Z−2,A−4 ·
  IT→바닥상태). 반감기·종점·감마상수는 전부 우리 자료에 있다.
  ★ **분기비는 자료에 없으므로 한 줄도 쓰지 않는다.**
· **평형을 한 덩어리로 다루지 않는다** — 그러면 반대 방향의 거짓이 된다. 딸 반감기 30일
  이하는 `present`(손에 쥐는 선원에 늘 있다), 그보다 길면 `ingrowing`(분리 시점을 모르면
  있다고 말할 수 없다). 쪽도 그렇게 갈라 말한다.
· ★★★ **절대 하한이 없으면 0 과의 비교가 아무거나 통과시킨다.** 모핵종이 알파 방출체면
  베타가 0 이라 **어떤 베타든 「1.3배 위」**가 되어, Pa-231 쪽이 「Ac-227 이 **0.0448 MeV**
  베타를 들여온다」고 적고 있었다 — 45 keV 는 선원 피복도 못 나간다. 하한을 넣어
  (베타 300 keV · 감마 0.01) **11장 → 7장**이 됐다. **하한이 곧 이 모듈의 정의다.**
· ★★ **한 걸음만 본다 — 그리고 숨기지 않는다.** Th-230·U-232 의 실제 위험은 한 걸음 아래가
  아니라 **그 밑의 연쇄**다. 한 걸음짜리 값을 쓰면 100배 규모로 과소 말하게 되므로
  하한에 걸려 떨어지는 것을 **되살리지 않는다.** 범위 밖인 것에 쪽은 아무 말도 하지 않는다.

### 두 번째 결함 — 연쇄가 **이 자료에서 끊기는데** 쪽이 결론을 냈다

Ra-226 쪽이 이렇게 소개하고 있었다 — 「The air kerma rate constant is **small** — 0.000879,
**88× less than Cs-137** … **ranking 80 of 96** … 22.8 GBq 라야 20 µSv/h」. U-238 쪽은
「**Shielding barely arises**」로 끝맺었다.

그 값은 **그 핵종 자신의 것**이고, 라듐·우라늄 선원의 광자장은 거의 전부 **자손**에서 나온다.
그런데 **Rn-222 · Pb-214 · Bi-214 · Th-234 가 이 자료에 한 종도 없다**(라돈은 원소 자체가 없다).

★★★ **Ru-106 때와 결정적으로 다르다 — 맞는 값을 우리가 만들 수 없다.** 그러면 고치는 길은
값을 지어내는 것이 아니라 **주장을 거두는 것**이다. 「이 자료에 그 다음이 없다」는 **우리 자료에
대한 진술**이라 물리를 기억으로 쓰지 않아도 참이다.
· 범위는 **알파 붕괴 + Γ>0 + 딸이 자료에 없음** = **4장**(Ra-226 · Ra-223 · U-235 · U-238).
  ★ **베타로 넓히지 않는다** — 베타에서 딸이 자료에 없는 것은 대개 **안정 핵종**이라
    (Co-60 → Ni-60) 「연쇄가 이어진다」가 그쪽에서는 거짓이 된다. 안정 여부는 우리 자료로
    알 수 없고, **알 수 없는 것에는 말하지 않는다.**
· 거둔 것: 「small」 · 다른 핵종 대비 배수 · **순위**(남들은 상수가 곧 전체 장인데 이 쪽만
  아니므로 특히 나쁘다) · 「몇 GBq 라야 20 µSv/h」 · 「Shielding barely arises」 ·
  「이미 20 µSv/h 아래」. **두께 값은 그대로 둔다** — 계산은 맞다.
· ★ **뒤에 단서를 붙이는 것으로는 모자랐다** — 앞 문장이 이미 판단을 준다.
· ★ 딸의 **이름을 못 낼 수 있다**: 원소기호는 이 자료에 있는 핵종에서만 나오는데 라돈은
  한 종도 없다. **주기율표를 기억으로 적지 않고 이름 없이 말한다.**

### 셋째·넷째 — 같은 물음을 전수로 물었더니 둘이 더 나왔다

**물음 하나로 훑었다: 「이 숫자가 실제 선원의 것인가?」** 결론을 내는 문장 13개를 전부 놓고
하나씩 물었더니 둘이 더 걸렸다.

**③ 「the limit here」가 쪽 전체로 번졌다 — 12장.** 광자장이 있는 알파 방출체가 **납 두께를
주면서 동시에** 「None of it reaches through skin, so **the limit here is intake, not external
dose**」라고 말하고 있었다. **쪽이 스스로 모순**이다 — Am-241 은 59.5 keV 때문에 감마 선원으로
쓰이고 Ra-226 은 고전적인 외부 위험이다.
★ 맞는 것은 **알파에 대한 진술**이었다. 「None of it」의 `it` 은 알파인데 **결론만 쪽 전체를
  가져갔다.** 광자가 있으면 거기까지만 말하고 나머지는 위 수치에 맡긴다.
  Γ=0 인 쪽(Po-210 등)은 원문 그대로다 — 거기서는 그 말이 맞다.

**④ 제동복사도 모핵종의 종점으로 계산됐다 — 6장.** Sr-90 쪽이 0.546 MeV 로 「납에서 **1.57%**」
라고 적는데, 실제 선원에서 그 X선을 내는 것은 **Y-90 의 2.28 MeV**(**6.54%**)다.
Ce-144 는 0.91% 대 **8.60%** 로 **9.5배**였다.
★★ 이것이 특히 나쁜 이유: **베타를 납으로 막으면 안 되는 이유가 정확히 제동복사**다.
  값이 낮게 나가면 **결론이 통째로 뒤집힌다.**
★ 고침은 둘 — 모핵종 문장은 **누구의 에너지인지 밝히고**(`Of Sr-90's own beta energy`),
  딸의 값은 **연쇄 문단이 든다**(`The shield is sized on Y-90, and so is the bremsstrahlung`).

★★★ **셋 다 같은 뿌리다** — 계산은 맞고 **그 계산이 무엇에 대한 것인지**가 틀렸다.
  낱장이 「이 핵종」을 계산해 놓고 **「이 선원」에 대해 결론**을 냈다.
  ★ 그래서 앞으로 결론 문장을 더할 때 물을 것은 하나다 — **「이 숫자가 실제 선원의 것인가?」**

**게이트 `npm run check-progeny`**(CI 포함). 목록의 쪽이 ① 딸 이름을 부르는가
② `present` 면 **딸에 맞춘 수치**까지 적는가 ③ 목록 밖 쪽이 남의 연쇄 문장을 들고 있지 않은가.
★★★ **첫 판은 ②를 재지 못했다** — 딸 이름 이후 **쪽 끝까지**를 봐서, 아래 표에 있는
  **모핵종의** 「1.57 mm of acrylic」이 걸려 **수치를 지워도 통과**했다. 역테스트에서 드러났고
  `<p>` 문단 단위로 좁혀 고쳤다. **「시야에 넣었다」와 「재고 있다」는 다른 층이다.**
★ **못 보는 것**: 문장이 맞는지 · **하한값이 맞는지**(정의라 기계가 못 잰다) · 여러 걸음 연쇄.

★ **조립된 영문 문장을 읽어 셋을 잡았다**(게이트가 전부 통과한 상태에서):
  `7.0e+2×`(`toPrecision` 이 1000부터 지수 표기로 넘어간다) · 위의 「0.0448 MeV 베타」 ·
  이성질체 전이인데 「chemically separated」라고 한 것(같은 원소라 화학 분리가 성립하지 않는다).

## ★★ 사이트맵 `lastmod` — 얕은 클론은 **그럴듯한 틀린 답**을 낸다 (2026-09-23)

사이트맵 166주소에 `lastmod` 가 하나도 없었다. 쪽마다 **그 내용이 마지막으로 바뀐 커밋
날짜**를 넣는다. 정본은 `src/lib/lastmod.mjs`, 게이트는 `npm run check-lastmod`(CI 포함).

· **핵종 낱장 147장의 원천은 생성기다** — `data/nuclides.json` · `lib/nuclide-prose.ts` ·
  `lib/nuclide-seo.ts` · `pages/nuclides/[slug].astro` 중 **가장 최근**. 그 글은 사람이 쓴
  것이 아니라 **생성물**이라 생성기가 바뀌면 147장의 글이 실제로 바뀐다. 붙박이 쪽은 그
  `.astro` 한 장이고, **틀(레이아웃·공통 컴포넌트)은 일부러 세지 않는다.**
· ★★ **빌드 시각을 전 쪽에 박지 않는다.** 구글은 믿을 수 없는 `lastmod` 를 **무시**하므로
  하나가 거짓이면 **넣은 것 전부가 무효**가 된다. **없으면 지어내지 않고 비운다** —
  부분 `lastmod` 는 정상이다(크롤러는 주소마다 따로 읽는다).
· ★★★ **얕은 클론(shallow)을 반드시 가려낸다.** 경계 커밋보다 오래된 변경은 **전부 그 한
  커밋으로 뭉쳐** 보인다 — RadiMeter 에서 실측했더니 **33쪽 중 17쪽이 실제보다 5~10일
  최신인 날짜**를 달았다(실제 9/08·9/11 → 「9/12」). 얕은 클론은 **오류를 내지 않는다.
  그럴듯한 틀린 답을 낸다.** `.git/shallow` 의 **경계 SHA** 에 걸린 파일은 「모른다」로
  버린다(날짜가 아니라 SHA 로 비교한다). **CI 체크아웃을 `fetch-depth: 0`** 으로 둔 이유다.
· 게이트가 무는 자리 셋 — 적힌 날짜가 **실제 커밋에 있는가** · 미래가 아닌가 ·
  **전 주소가 같은 날짜가 아닌가**. **역테스트 셋**으로 확인했다.
  ★ **못 보는 것**: 매핑이 엉뚱한 파일을 짚어도 그 날짜는 실제 커밋이라 통과한다 — 눈으로 본다.

★★★ **같은 날 내가 낸 오진을 여기 남긴다 — 「라이브 사이트맵이 10쪽을 빠뜨렸다」는 틀렸다.**
라이브가 핵종 137장인데 허브는 147장을 링크하는 것을 보고 **낡은 산출물**이라고 결론했다.
근거로 「`origin/main` 을 워크트리로 빼서 구우니 148 이 나온다」를 들었는데,
**그 `origin/main` 이 낡은 ref 였다.** 실제 main 에는 그 전날 들어온 커밋 셋이 있었다 —
`68bac8c`·`b2c6b9c`·`dd90977`(2026-09-22 00:20~00:22). 그 열 장은 **반감기 1초 미만
이성질체**이고 **일부러 `noindex` 로 빼고 사이트맵에서도 거른 것**이다(정본
`src/lib/nuclide-noindex.ts`). 라이브 156 은 **낡은 것이 아니라 맞는 값**이었다.
★ 무엇이 나를 속였나: 소프트 404 도, CDN 캐시도, 우산 가로채기도 아님을 **네 단계로
  확인하고** 「그러니 산출물이 낡았다」로 갔다. **후보를 넷 지웠다고 다섯째가 없는 것이 아니다.**
★★ 교훈은 같은 날 `lastmod` 에서 배운 것과 **글자 그대로 같다** — 「이력에서 값을 뽑을 때는
  **이력이 다 있는가를 먼저 묻는다**」. 거기서는 얕은 클론을 의심했으면서, **ref 가 최신인지는
  묻지 않았다.** 비교의 기준으로 쓰는 것은 **쓰기 전에 `git fetch` 한다.**
★ 그래서 이 변경은 그 열 장을 **되살리지 않는다.** 병합에서 main 의 필터를 그대로 두고
  `lastmod` 만 얹었다(실측: loc 156 · lastmod 156 · 뺀 열 장 유출 0).

★ **기대치**: `lastmod` 는 「다시 올 때」를 정하는 신호이지 「처음 올 때」를 당기는 신호가
아니다. 서치콘솔의 「발견됨 — 색인 안 됨 171」은 **한 번도 크롤된 적이 없는** 쪽들이라
`lastmod` 가 그 숫자를 직접 밀지는 못한다. 첫 크롤을 당기는 것은 **내부 링크 ·
소유주의 색인 생성 요청 · 시간** 셋이다.

## 절대 규칙

1. **상수표를 베끼지 않는다 — 공개 원천에서 계산한다.**
   감마상수 Γ 는 핸드북 표가 아니라 `Σ Eᵢ·yᵢ·(μen/ρ)air / 4π` 로 IAEA(방출선)와
   NIST(감쇠계수)에서 직접 계산한다. 값이 우리 것이 되고 **검증도 된다**.
2. **유료 규격 본문을 재현하지 않는다.** ANSI/ANS-6.4.3 의 GP 빌드업 계수표가 여기 걸린다 —
   Berger 2-계수형과 사용자 입력으로 우회한다(`engine/gamma.ts`).
3. **문헌값을 기억으로 적지 않는다.** 이 레포를 세우는 동안 기억으로 적은 기준값 **넷이 틀렸고**
   (Na-22 Γ·Mn-54 Γ·Zn-65 Γ·Pu-241 비방사능) 그때마다 **계산 쪽이 옳았다**.
   테스트의 기준값은 출처가 확실한 것만 쓴다.
4. **면책 없이 결과를 내보내지 않는다.** 공인 교정·시험·차폐설계를 대체하지 않는다.
5. **공개 콘텐츠는 영문.**

## 데이터 원천

| 데이터 | 원천 | 라이선스 |
|---|---|---|
| 반감기·붕괴형식·감마/X선 에너지와 방출강도 | IAEA Live Chart API `rad_types=g` | 출처 표기 |
| 베타 E_max·E_평균·가지 방출강도 | 같은 API `rad_types=bm`·`bp` | 출처 표기 |
| 알파 에너지·방출강도 | 같은 API `rad_types=a` | 출처 표기 |
| 질량감쇠계수 μ/ρ · 질량에너지흡수계수 μen/ρ | NIST **SRD 126**(Hubbell & Seltzer, NISTIR 5632, `doi:10.18434/T4D01F`) | **공용저작물 아님** — 아래 ★★ |

★★ **NIST 를 「public domain」이라 적고 있었다 — 틀린 주장이었다**(2026-09-16, 「인용하는 법」을
쪽에 실으려고 원문 약관을 열어 보다 잡았다). 그 표는 **NIST Standard Reference Database 126** 이고
쪽에 이렇게 적혀 있다 — 「© 1989, 1990, 1996 copyright by the U.S. Secretary of Commerce …
**All rights reserved.** NIST reserves the right to charge for these data in the future.」
SRD 는 **15 U.S.C. §290e** 로 별도 권리가 붙어 미국 정부 저작물의 공용 원칙(17 U.S.C. §105)에서
**벗어나 있다.**
· **제품에 넣어 계산에 쓰는 것**과 **표 자체를 데이터셋으로 재배포하는 것은 다른 층**이다.
  지금 하는 것은 앞쪽이고, 뒤쪽을 하려면 NIST 에 확인할 것(`data@nist.gov` · `nist.gov/srd`).
· IAEA 는 반대로 **명시적으로 허용**한다 — 약관이 데이터의 「use, reproduction and dissemination」을
  장려하고 **상용 제품에서의 사용까지** 허용하며, 조건은 **출처 표기**와
  **「IAEA 가 보증한다고 말하거나 암시하지 않을 것」** 둘이다.
· 서지·조건의 정본은 **`brand.ts` 의 `SOURCE_CITATIONS`** 한 곳이고 `/methods/` 가 그것을 그린다.

**147 핵종** — 감마 96 · 베타 35 · 알파 33, 그중 **감마를 안 내는 것 45종**
(H-3·C-14·Ni-63·Sr-90·Y-90·Tc-99·I-129·Zr-93·Se-79·Po-210·Pu-241/242·Np-237·Cm-245 …).
폐기물·해체·내부피폭에서 실제로 세는 것들이다.

수확 스크립트는 `scripts/harvest.py`(핵종) · `scripts/nist.py`(감쇠계수) · `scripts/fill.py`
(순수 베타·알파 보충). 산출물은 `src/data/` 에 **커밋된다** — 빌드 시점에 외부를 치지 않는다.

★ **수확에서 실제로 밟은 함정 셋** (전부 조용히 틀린 값을 낸다):
- `rad_types=g` 응답에 **X선이 이미 들어 있다.** `x` 를 따로 받아 합치면 Am-241 이 **16배** 튄다.
- 한 요청에 **이성질체 부모가 섞여 온다.** `p_energy` 로 가르지 않으면 Ir-192 가 **+293%**.
  (덤: Tc-99m=142.68 · Ba-137m=661.66 이 이 경로로 정확히 잡힌다.)
- **δ 를 데이터에 구워 넣지 말 것.** 선을 20 keV 에서 잘라 저장했더니 화면의 δ 손잡이가
  아무 일도 못 했다. 선은 1 keV 까지 싣고 **δ 는 계산 시점에** 건다.
- **이성질체는 베타·알파에도 섞인다** — Kr-85 에 Kr-85m(T½=4.48h)의 840 keV 가 붙어
  E_max 가 **+22%** 틀렸다. `p_energy` 필터를 **세 rad_type 전부에** 걸어야 한다.
  지키는 것은 `data.test.ts` 의 **방출강도 합 ≤105%** 검사다(섞이면 합이 먼저 튄다).
- **NIST 쪽은 같은 표를 두 번 낸다**(인쇄용이 따라붙는다). 자르지 않으면 표가 두 벌이 되고
  **보간은 앞쪽에서 끝나므로 값이 맞고 조용하다** — 에너지축 단조성 검사로만 드러난다.
- **`gamma_const > 0` 으로 방출을 가르지 말 것** — Fe-55·Ge-68 처럼 광자가 전부 δ 아래인
  EC 핵종이 **통째로 사라진다**. 오염검사에서 실제로 세는 것들이다.

## 검증 상태 (`npm test` 143건)

| 항목 | 결과 |
|---|---|
| NIST 감쇠계수 5점 | 문헌과 **0.5% 이내** (662 keV 는 표점 사이 보간이라 1.5%) |
| Γ — Cs-137·I-131·Cs-134·Co-60·Ir-192 | 문헌과 **2% 이내** (δ=20 keV) |
| 비방사능 — Co-60·Cs-137·Pu-239·U-238·Am-241·Sr-90·H-3 | 문헌과 **1% 이내** |
| β E_max — H-3·C-14·P-32·S-35·Ca-45·Ni-63·Sr-90·Y-90·Tc-99·Pm-147·Cl-36·Tl-204·Kr-85·Pu-241·I-129 | **1% 이내** (15/15) |
| α 주선 — Am-241·Pu-239·Po-210·U-238·Ra-226·Cm-244·Pu-238·Th-230 | **0.01% 이내** (8/8) |
| 붕괴·역산·Bateman·MDA·스캔 MDC·역제곱·비정·제동복사 | 해석해·경험식과 일치 |
| 단위환산 — **군마다 바깥 기준으로 확인한 쌍 16개** + 농도 군 분모 검사 | 정확값과 1e-9 이내 |
| 데이터 무결성 (이성질체 혼입·표 중복·EC 분류·δ 보존) | 게이트 8건 |
| **단위환산 유효성 평가** — 단위 44개 배수를 1차 원천에서 손계산, 순서쌍 **276개 전수** | 최대 상대차 **1.455e-16** (합격기준 1e-12) |
| **붕괴 유효성 평가** — 항등식 14종 × 반감기 5개, 반감기 **24종을 DDEP 와 대조** | 수학 **2.2e-16** · 반감기 최대 **2.82σ**(한도 3σ), 중앙값 차이 0.033% |
| **감마 유효성 평가** — 단위 사슬 손 유도 · 표점 **602개** 전수 · 방출선 **6선을 DDEP 에서 유도** | 유도 **0** · 표점 **5.8e-16** · 방출선 최대 **0.58σ** |
| **비방사능 유효성 평가** — 몰 질량 **147종 전수를 AME2020 과 대조** · 유도 4 · 항등식 6 · 손계산 6 | 수학 **2.2e-16** · 저장값 재계산 **0** · 질량결손 항등식 **5.72e-11** · ★ **질량수 근사 결함을 찾아 제거**(최대 0.535%) |
| **베타 유효성 평가** — 에너지보존은 정확, **경험식 셋**은 형태·범위·ESTAR 대조 · 「세우지 못한 것」 5건 명시 | 수학 **0** · 형태 **2.2e-16** · 우회인자 **0.454–0.794**(39점) · ★ **엔진 결함 넷**(가지 경계 · 범위 가드 · NaN 가드 둘) |
| **검출하한 유효성 평가** — **MARSSIM 이 본문에 실은 수치 예제 셋**을 재현 · 상수 차이 실측 | 예제 **414·372 자리까지 일치** · 셋째는 **MARSSIM 자신의 반올림**(755.9 vs 750) · ★ **d′ 의 뜻이 틀려 있었다** · 거부 6자리 |
| **작업계획(ALARA) 유효성 평가** — **대조할 바깥 자료가 없는 도구** · 가정을 거리 기준으로 환산 | 기하·산술 **0**(1024건) · 점근사 과대평가 **d=L 7.84% → d=3L 0.92%** · ★ **거부 아홉 자리 전부 뚫려 있었다** |

★ **δ=20 keV 가 저에너지 방출체의 답을 좌우한다** — δ=10 으로 두면 Am-241 이 **+805%** 틀린다.
캡슐에 흡수되어 거리에서 선량에 기여하지 않는 광자를 빼는 관행이고, **화면 손잡이로 노출한다**.

## 구조

```
src/engine/   계산엔진 — 순수 함수, 의존성 0. 테스트가 같은 폴더에 산다
  constants · interp(로그-로그) · gamma(Γ·차폐·빌드업) · decay(붕괴·비방사능·Bateman)
  mda(Currie·스캔) · alara(역제곱·체류시간) · units(군을 섞지 않는 환산 — 질량농도·부피농도가 별개 군)
  beta(무한매질 선량률·Katz–Penfold 비정·제동복사)
src/data/     커밋된 데이터 — nuclides.json(117 핵종) · attenuation.json(8 재료 722행)
scripts/      원천에서 다시 수확하는 스크립트
```

## 방문 집계 — 플랫폼 층이고, 이 레포는 한 줄만 든다 (2026-09-15)

소유주 지시로 lab 넷 전부에 방문 집계를 세웠다. **정본은 우산 저장소**
(`aijhmin1031-eng/radiation-lab`)의 `public/track.js` 한 벌이고, lab 넷이 **같은 파일**을 읽는다.
경위·스키마·utm 낱말 규약은 **그 레포 README 가 정본**이다.

이 레포가 든 것은 `src/layouts/Base.astro` 의 한 줄이다 —

```astro
<script is:inline src="/track.js" data-lab="calc" defer></script>
```

· ★★ **`base` 를 붙이면 안 된다.** `/calc/track.js` 는 **없다** — 그것은 우산이 오리진 루트에
  두는 파일이다. base 를 붙이면 **404 가 나고 빌드는 통과하며 집계만 조용히 죽는다.**
  그래서 `is:inline` 으로 Astro 가 손대지 못하게 둔다.
· ★ **게이트 둘을 손봤다**(둘 다 이 변경을 **옳게** 막고 있었다) —
  ① `gate/check-output.mjs` 의 `EXACT_OK` 에 `/track.js` 를 더했다. base 밖을 가리키는 것이
     **맞는** 자리다(「있을 법한 주소」가 아니라 **우산 저장소에 실재하는 파일**이다).
  ② `gate/check-render.mjs` 의 로컬 서버가 `/track.js` 에 **200 을 답한다.** 그전에는 404 라
     **전 쪽에서 「JS 오류」로 세어져 게이트가 통째로 빨개졌다.** 무시하지 않고 **흉내 낸다** —
     「없는 것을 못 본 척」은 태그가 사라져도 통과하지만, **배포 구조를 재현하는 것**은 안 그렇다.
· ★ **공유 줄에 utm 을 달았다**(`ShareRow.astro` 다섯 자리 전부, **「링크 복사」 포함**).
  referrer 만으로는 「어디서 왔는가」가 안 나온다 — 앱 내 브라우저는 referrer 를 비워 보내고,
  메신저·이메일로 옮겨 붙인 링크는 원래부터 없다. **낱말은 우산 README 가 정본이다.**
· 쿠키·localStorage 를 쓰지 않는다(순 방문자는 서버측 일일 해시) — **동의 대상이 아니다.**
  그래서 이 lab 에 동의 배너를 새로 세울 필요가 없었다.
· 보고서는 **`radiation-lab.com/stats/`** 다(로그인 + 우산 DB 의 `rl_admins` 에 든 계정).

★ **`*.vercel.app` 으로 직접 들어온 방문은 세어지지 않는다** — 그 오리진에는 `/track.js` 가
없다. 정본 주소가 우산이므로 그것이 맞는 동작이다.

## ★★ 껍데기는 플랫폼 정본이다 — 이 lab 의 팔레트가 한 칸 밀려 있었다 (2026-09-16)

같은 날 두 번 손댄 자리다. **앞 판의 진단이 절반만 맞았다** — 아래에 경위를 남긴다.

**① 오전** — 소유주 지적 「여기는 메뉴와 본문 배경색이 비슷해보여」. 머리글이 `bg-panel`
(순백)이라 본문(251,251,250)과 채널당 4밖에 차이가 없었다. 머리글 전용으로
`--c-chrome`(241,242,238)을 **새로 만들어** 붙였고, 「껍데기는 본문이 밝으면 어둡게 —
방향이 lab 마다 다른 것이 정상」이라고 적었다.

**② 오후** — 소유주 지적 「radcalc과 radimeter lab 화면 배경색 구성이 다르네 … 상단메뉴는
동일한 배경, 폰트, 크기를 사용하면 좋겠어.」 **①의 그 문장이 틀렸다.**

| | 본문 | 머리글 | 관계 |
|---|---|---|---|
| 우산 | 232,234,230 | 241,242,238 | 머리글이 **밝다** +25 |
| RadiMeter | 232,234,230 | 252,252,250 | 머리글이 **밝다** +58 |
| 이 lab (①의 결과) | **251,251,250** | 241,242,238 | 머리글이 **어둡다** −31 |

· ★★ **원인은 색이 아니라 토큰 이름이 한 칸씩 밀린 것이었다.** 우산·RadiMeter 는
  `paper`=본문 · `surface`=카드 · `panel`=머리글인데, 이 lab 에는 **`--c-paper` 가 없어**
  `--c-surface` 가 본문을 맡고 `--c-panel` 이 순백 카드였다. ①에서 **우산과 같은 값을
  골랐는데도 방향이 반대가 된 이유**가 그것이다.
· ★★ **그래서 값이 아니라 이름을 맞췄다.** `--c-paper` 를 세워 본문에 주고, 카드·입력칸·
  단추를 `--c-surface` 로, 머리글·꼬리말·압축 고정 바를 `--c-panel` 로 옮겼다.
  **`--c-chrome` 은 없앴다** — 머리글 색의 이름은 플랫폼 어디서나 `--c-panel` 이다.
  값만 맞추면 다음 사람이 `bg-panel` 을 「카드 색」으로 읽고 다시 어긋난다.
· ★★ **바탕을 옮기면 글자 대비가 함께 움직인다.** 본문이 251 → 232 로 어두워지며
  `--c-ink-faint` 가 **4.86 → 4.16 으로 떨어졌다**(AA 하한 4.5). 색은 그대로 두고 바탕만
  옮기면 **빌드도 통과하고 눈으로도 안 보이는 채로 접근성이 깨진다.** 101,105,114 로
  내렸다(본문 4.54 · 머리글 4.89 · 카드 5.36). **팔레트를 옮길 때는 대비를 다시 계산할 것.**
· **칸 폭도 맞췄다** — `max-w-content` 를 72rem(1152) → **1048px**, 좌우 여백 16 → 32px
  (모바일 20px). 그전에는 글줄이 좌측 **80px** 에서 시작해 우산·RadiMeter(둘 다 148px)와
  어긋났다 — 오리진을 넘나들면 본문이 68px 왼쪽으로 튀었다.
· **메뉴 칸도 맞췄다** — 13px · w400 · 모서리 4px · 안여백 6·12px · 활성은 앰버 알약.
  ★ `rounded-lg` 는 이 lab 에서 8px, RadiMeter 의 체계에서는 4px 이다 — **같은 클래스
  이름이 두 배 다른 값**으로 풀린다. 이름이 아니라 **값**을 맞춘다.
· ★ **머리글·고정 바가 같은 목록을 읽는다.** 그전에는 링크가 두 곳에 복사돼 있었고
  그 바람에 **`Saved`·`Methods` 에 활성 표시가 아예 없었다**(`/methods/` 에서 켜지는 칸 0개).
· ★★ **「층 대비 18 이상」 검사가 이것을 못 봤다 — 그것은 상대값이다.** 갈리기는 갈리는데
  **서로 다른 방향으로** 갈렸으므로 셋 다 통과했다. 새 게이트 `gate/check-platform.mjs` 는
  **절대값**을 잰다(본문·머리글·아래선·칸 좌측·칸 치수·활성 알약, 라이트/다크).
  역테스트 둘로 확인했다. 정본 값은 우산 README 「껍데기는 플랫폼 정본 한 벌이다」 절이다.
· ★ **교훈**: 「방향이 lab 마다 다른 것이 정상」처럼 **차이를 설명해 버리는 문장**을 조심할 것.
  ①에서 그 문장을 적는 순간 이 결함은 **문서에 의해 정당화**됐다.

## ★★ 단위환산이 부피↔질량을 밀도 없이 건넜다 — 1000배 (2026-09-16)

전면 검토에서 잡았다. `massConc` 군에 **`Bq/L`·`pCi/L` 이 질량 배율로** 들어 있었다.
배율이 「1 L = 1 g」이라 답이 **정확히 1000배** 틀렸다 —

| 넣은 값 | 그전 답 | 옳은 답(물, 1.00 g/mL) |
|---|---|---|
| 1 Bq/L → Bq/kg | **1000** | 1 |
| 1 Bq/L → Bq/g | **1** | 0.001 |
| 1 pCi/L → Bq/kg | **37** | 0.037 |

· ★★ **이 레포의 원칙을 이 표 자신이 어기고 있었다.** `/units/` 본문이
  「This converter therefore **refuses to cross between quantities** … Converting between them is
  **physics**」라고 못 박아 두고, 정작 분모가 다른 두 양을 **밀도 없이** 건너뛰고 있었다.
  `pCi/L` 은 먹는물 기준이 쓰는 단위라 실무에서 바로 밟는 자리다.
· ★★★ **게이트가 구조적으로 못 봤다 — 「자기 자신과 왕복」만 보고 있었기 때문이다.**
  왕복은 **배율이 무엇이든 성립한다**(`(v×k)/k = v`). 그래서 1000배 틀린 값이 전 테스트를
  통과했다. 부피끼리(`pCi/L`→`Bq/L`)는 비가 맞아 화면으로도 안 보였다.
  **되풀이되는 교훈이다: 비교하는 자가 실제 실패 모드를 재는지 확인할 것.**
· **고친 방식은 값이 아니라 군이다** — `Bq/L` 의 배율을 `1e-3` 으로 고치면 숫자는 맞지만
  **「밀도 1.00 을 몰래 가정한 표」**가 되어 다음 사람이 다시 밟는다. 부피농도를
  **자기 군(`volConc`)으로 갈랐고**(`Bq/L`·`kBq/L`·`MBq/L`·`Bq/mL`·`Bq/m³`·`pCi/L`·`µCi/mL`),
  질량↔부피는 **밀도를 받는 물리 단계**로 뺐다(`massConcFromVolConc` — 조사선량↔공기커마와 같은 꼴).
  화면에 밀도 손잡이(기본 1.00 g/mL)와 「**not a unit conversion**」 경고를 함께 세웠다.
· **지키는 것은 테스트 셋이다** — ① 군마다 **바깥 기준으로 확인한 쌍 16개**(왕복이 못 보는 자리)
  ② **농도 군의 분모 검사**(`massConc` 는 g·kg 만, `volConc` 는 L·mL·m³ 만, 겹치는 단위 0)
  ③ 표가 군을 건너면 **NaN** 이라는 단정.
  ★ ①을 쓰다가 내가 적은 기대값 하나(`dpm/100cm²`→`Bq/m²`)가 **10배 틀렸고 그 자리에서 잡혔다** —
    검사가 실제로 일을 한다는 증거다(1.667 이 맞다).
· 곁다리 둘 —
  · **`npm run typecheck` 가 실행조차 안 되고 있었다.** `@astrojs/check` 가 devDependencies 에
    없어 대화형 설치 프롬프트에서 멈춘다(CI 는 `tsc` 만 돌려 안 걸렸다). 넣고 돌리니
    **진짜 오류 하나**가 나왔다 — 알파선 표의 `[...n.alpha]` 는 `Boolean(n.alpha?.length)` 로
    지켜지지만 **타입은 그 좁힘을 JSX 경계 밖으로 옮기지 못한다.** `?? []` 로 풀었다.
  · **`calc_insert_own` 이 `with check (true)`** 였다 — 트리거가 `user_id` 를 덮으므로 결과는
    안전하지만 **방어선이 하나**다. 정책도 같은 것을 확인하게 했다(`sql/004_insert_check.sql`,
    적대적 확인 포함). **소유주가 SQL 에디터에서 한 번 실행해야 반영된다.**

## ★★ 인용·Zenodo — 원천 조건을 원문에서 확인했다 (2026-09-16)

소유주 지시: 「how to cite 부터 해보자. zendo 까지 준비해봐.」

· **`/methods/` 에 「How to cite」 절을 세웠다.** 서지·조건의 정본은 **`brand.ts` 의
  `SOURCE_CITATIONS`** 한 곳이고 쪽이 그것을 그린다 — 쪽마다 글자로 적지 않는다.
  ★ **우리 사이트보다 원천을 먼저 인용하라고 적었다** — 평가 자료는 IAEA·NIST 의 일이고
    인용의 값어치는 그쪽에 붙어야 한다. 우리를 인용할 자리는 **파생**을 쓸 때다
    (Γ · 스펙트럼 반가층 · 선별 커마 기여도는 여기서 계산되고 이 꼴로는 여기에만 있다).
· ★★ **그 절을 쓰려고 약관을 열어 보다 이 레포의 틀린 주장을 잡았다** — 위 「데이터 원천」 표가
  NIST 를 **public domain** 이라 적고 있었다. 실제로는 **SRD 126** 이고 「All rights reserved」다.
· **Zenodo 준비물은 `docs/zenodo.md` 가 정본**이다 — 막혀 있는 결정 둘(저자 이름·라이선스),
  원천 조건 원문, 무엇을 올리고 무엇을 안 올리는지, 절차, 점검표.
  저장소에 `CITATION.cff` 와 `.zenodo.json` 을 두었다(**이름·라이선스는 `TODO` 로 비워 두었다** —
  지어낸 이름이 DOI 와 함께 영구 보관되면 되돌릴 수 없다).
· **`npm run make-dataset`** 이 배포물(`dist-dataset/`)을 만든다. 요점은 **NIST 원표를 빼는 것**이고,
  사람이 기억으로 지키면 반드시 새므로 **만드는 자리에서 빼고 만든 뒤 훑는다.**
  ★ 검사를 **둘로 가른다** — ① 수치가 실려 있으면 **결함**(재배포다) ② 참조만 있으면
    **수확 단계가 필요하다는 뜻**이므로 README 가 그 절차를 들고 있어야 한다.
    처음에 섞어 세었더니 `import` 문 네 개가 결함으로 잡혔다 — 다른 사실을 같은 자로 재고 있었다.
  ★★ **그 지문을 글자로 비교했다가 역테스트에서 안 잡혔다** — 파일에는 `5210.0` 인데 파싱하면
    `5210` 이라 글자열이 다르다. **숫자열로** 견주도록 고쳤고, 서식만 바꾼 사본(지수 표기)도
    잡는 것을 확인했다. 되풀이되는 교훈이다: **비교하는 자가 실제 실패 모드를 재는지
    역테스트로 확인할 것.**

## ★★ 핵종 낱장 147장 — 이 lab 이 남들과 갈리는 자리 (2026-09-16)

소유주 질문: 「radcalc 이 사이트에 우리만의 특별한 기능을 추가할 것이 뭐가 있을까? **유사한 사이트가
너무 많아**」. 무료 경쟁 도구를 열어 보니 구성이 거의 같았다 — 단위환산·붕괴·감마 선량률·차폐,
그리고 **전부 핵종마다 감마상수 Γ 하나**를 쓴다(RadMaths 는 베타 핵종이 목록에 아예 없다).

**우리가 이미 들고 있는데 화면이 안 보여 주던 것이 답이었다.**

| 우리가 가진 것 | Γ 상수 기반 도구가 못 하는 이유 |
|---|---|
| 147핵종 **전체 방출선 표** | Γ 는 스펙트럼을 숫자 하나로 뭉갠 값이라 선별 차폐를 못 한다 |
| **Γ = 0 인 51종**(C-14·Ca-45·Cs-135·Cm 계열…) | Γ 기반 도구는 0 을 돌려주거나 목록에 없다 |
| NIST μ/ρ 8물질 | 반가층을 표에서 읽지 않고 **푼다** |

### 스펙트럼 반가층 — `src/engine/spectrum.ts`

★★ **반가층은 스펙트럼에 걸린 값이다.** 선이 여럿이면 각자 다른 μ 로 줄고, 얇은 층에서
연한 선이 먼저 죽어 투과곡선이 **단일 지수가 아니다**(빔 경화). 그래서 「대표 에너지의 반가층」은
근사이고, 실무가 쓰는 값은 **「이 핵종의 공기커마율을 절반으로 만드는 두께」**다. 우리는 IAEA 선 표와
NIST μ/ρ 를 둘 다 들고 있으므로 그것을 이분법으로 직접 푼다.

· **실측으로 확인한 것**(`src/engine/spectrum.test.ts`, 10건) — 납, 좁은 빔, 2026-09-16 실측:

  | 핵종 | 스펙트럼 풀이 | 단일선 지름길 | 지름길이 얼마나 두꺼운가 | TVL/HVL |
  |---|---|---|---|---|
  | Mn-54 | 7.184 mm | 7.184 mm | **0%** — 한 선이 커마의 100%, 해석해와 **오차 0**(검증 기준) | 3.32 |
  | Cs-137 | 5.345 mm | 5.496 mm | **3%** | 3.39 |
  | Co-60 | 10.349 mm | 10.832 mm | **5%** | 3.33 |
  | Na-22 | 6.530 mm | 10.525 mm | **61%** | 4.07 |
  | Sn-113 | 0.068 mm | 2.525 mm | **37배** | 84.40 |

  **Na-22** 가 대표 사례다 — 주선(1275 keV)이 커마의 **55.4%** 뿐이고 나머지가 511 keV 소멸감마다.
  이 차이가 우리가 스펙트럼을 푸는 이유이고, 게이트가 **그 차이가 있다는 것**을 잰다
  (사라지면 이유도 사라진 것이다).

  ★★ **이 차이를 %로 옮길 때는 분모를 반드시 밝힐 것**(2026-09-16). 같은 한 쌍의 값이
  **두 숫자**가 된다 — Na-22 를 예로:
  · **38.0%** = (10.525−6.530)/**10.525** … **단일선 값이 분모.** 「지름길 답의 38% 가 군더더기」
  · **61.2%** = (10.525−6.530)/**6.530**  … **옳은 값이 분모.** 「지름길이 61% 두껍다」
  둘 다 산수는 맞다. **틀리는 것은 한 글 안에 둘을 섞는 것**이고, 이 문서가 그냥 「38% 차이」라고만
  적어 둔 탓에 실제로 그 일이 났다 — 홍보 문안 초안이 **같은 글에서 「60% too thick」과
  「Na-22: 38%」를 둘 다** 말하고 있었다(게시 전에 잡았다).
  **위 표와 바깥에 내보내는 글은 「지름길이 몇 % 두꺼운가」(옳은 값 분모)로 통일한다** —
  독자가 알고 싶은 것은 「내가 쓰던 값이 얼마나 빗나갔나」이기 때문이다.
  ★ **Sn-113 은 %로 쓰지 않는다** — 3590% 는 숫자가 커서 오히려 안 믿긴다. **「37배」**로 쓴다.
  ★ 테스트는 **% 값을 단정하지 않는다**(`> 0.2`). 분모를 바꿔도 안 깨진다.
· **Sn-113 은 TVL/HVL 이 84 다.** 단일선이면 이 비가 정확히 3.3219 인데, 첫 반가층은 연한 광자를
  죽여 얻고 십가층은 단단한 꼬리를 뚫어야 한다. **「반가층을 세 번 겹치면 1/8」이 거짓**인 핵종이
  있다는 뜻이고, 낱장이 그 비를 인쇄해 독자가 알아보게 한다.
· ★★ **좁은 빔이다 — 문헌표와 다른 것이 정상이다.** Co-60 납 반가층이 우리 값 10.35 mm, 흔히
  인용되는 값 12.5 mm 인데 **틀린 것이 아니라 다른 양**이다(저쪽은 축적 포함). 문헌표에 맞추려
  들면 축적을 몰래 섞게 되고 화면의 「narrow beam」이 거짓이 된다. 그래서 테스트는 문헌표가 아니라
  **같은 정의의 해석해**와 대조한다. 축적 계수는 이 레포가 싣지 않으므로(유료 규격 본문)
  설계값이 필요하면 감마 차폐 계산기에서 계수를 넣는다.

### 147장을 한꺼번에 늘리는 것이 이 작업의 유일한 진짜 위험이었다

★★ **어수 검사는 「틀에 값만 갈아 끼운 글」을 못 본다.** `check-output` ⑥ 은 250어 하한만 보는데
**되풀이되는 설명으로 채워도 어수는 늘어난다.** 첫 판이 정확히 그랬다 — 어수는 최소 346어로
넉넉했지만 낱장 사이 5-그램 겹침이 **중앙 20.8%** 였다. 원인은 분명했다:
「narrow beam 이란 무엇인가」·「왜 저Z 로 막는가」 같은 **설명을 147번 되풀이**하고 있었다.

· 규칙을 세웠다 — **낱장의 문장은 「이 핵종의 숫자」와 「그 숫자에서 나오는 판단」만** 담는다.
  일반론은 **`/nuclides/` 한 곳**에 두고 링크한다(정본 한 곳). 겹침이 **중앙 9.9%** 로 떨어졌다.
· **쪽마다 다른 척하는 숫자를 쓰지 않는다** — 제동복사 수율의 「몇 배」는 Z 비율(82/6)이라
  **모든 핵종에서 같다.** 그런 숫자를 147번 찍는 것이 정확히 틀에 값만 갈아 끼우는 짓이다.
· 지키는 것은 **`node gate/check-nuclides.mjs`**(CI 포함). 겹침 중앙 상한 18% · 한 쌍 상한 55%.
  역테스트로 확인했다 — 설명을 되돌리자 **27.8%** 로 걸렸다.

### 게이트가 자기가 아는 것만 보던 구멍

★★ 처음에 계산기 링크를 **`?n=Co-60`** 으로 적었는데 **그것을 읽는 코드가 아무 데도 없었다**
(상태는 `?s=<base64 json>` 하나로만 들어온다). 눌러도 기본 핵종이 뜬다 — 이 lab 이 `Saved` 에서
이미 밟은 「없는 기능을 광고하지 않는다」와 같은 종류다. 지금은 `encodeState` 와 같은 꼴을
빌드 시점에 만들고, **브라우저로 눌러 값이 실제로 복원되는지** 게이트가 확인한다.

★★★ 그런데 **역테스트에서 게이트가 그 회귀를 놓쳤다.** 처음 판은 「`?s=` 가 있는 링크」를 세었는데,
죽은 `?n=` 링크는 그 패턴에 안 걸려 **검사 대상에서 빠지고 통과했다.** **검사가 자기가 아는 것만
보면 고장난 것은 늘 시야 밖에 있다.** 지금은 링크에 **`data-prefill` 표식**을 달고 표식을 세며,
표식 없는 **내부** 링크가 질의인자를 달고 있으면 그 자체를 결함으로 센다.
(★ 처음에 링크 전부를 봤다가 **공유 단추(X·Threads·LinkedIn)가 걸렸다** — 그쪽 인자는 남의
서비스가 읽는 것이다. 내부만 본다.)

### 그 밖에 실측으로 잡은 것

· `Technetium-99M` — 이성질체는 **소문자 m** 이다.
· `1 GBq 는 2.39e+4 ng` — 단위 눈금에 **위 경계**를 안 줘서 생긴 것(23.9 µg 가 맞다).
  자리수가 Tc-99m 의 5 pg 부터 Ca-41 의 0.3 g 까지 걸쳐 있다.
· `9.94E+4 years` — 자료의 `hl` 이 IAEA 표기 그대로라 지수가 섞여 있다. 사람이 읽는 꼴로 바꾼다.
· **Co-60 의 베타 종점 1.49 MeV 는 세기 0.12% 인 가지**에서 나온다(실제로는 99.88% 가 0.318 MeV).
  비정은 종점으로 잡는 것이 맞지만(가장 센 베타를 막아야 한다) **그 사실을 밝히지 않으면**
  독자는 그 에너지를 대표값으로 읽는다.
· **시간 눈금을 반감기에 맞춘다** — 6시간짜리에 「40년 뒤」는 늘 0 이고 10만년짜리에 「1년 뒤」는
  늘 100% 다. 둘 다 아무것도 알려 주지 않는다.
· ★★ **목록 표에서 같은 자리를 두 번 밟았다**(둘 다 소유주가 화면에서 잡았다).
  ① 148행 × 6열이 390px 에서 문서를 **269px 넘겼다** → 스크롤 컨테이너에 넣었다.
  ② 그랬더니 **오른쪽으로 밀면 핵종 이름 칸이 화면 밖으로 나갔다**(실측 left = −334px).
     남는 것은 「beta-minus decay · beta · —」뿐이고 **그 줄은 아무 뜻이 없다.**
  ★ 처음 고친 것은 첫 칸을 `position: sticky` 로 고정한 것인데, 화면을 찍어 보니
    고정된 칸 **옆으로 「nus」·「ecay」 같은 글자 꼬리**가 삐져나왔다 — 스크롤이 남아 있는 한
    어느 칸은 반쯤 잘린다. **390px 에 6열을 욱여넣은 것 자체가 무리**였다.
  ★ 그래서 **칸을 줄여 스크롤 자체를 없앴다** — 이름·붕괴형식·Γ 를 `lg:`(1024px)부터 보인다.
    좁은 화면에서 이 목록의 일은 **찾아서 누르는 것**이고, 셋은 낱장 맨 위에 다 있다.
    실측: 360·390·700·900·1024·1280px 전부 **가로 스크롤 0**. 320px 만 27px 남고
    거기서는 고정 칸이 일을 한다.
  ★★ **기존 게이트가 이것을 구조적으로 못 봤다 — 전부 스크롤 0 에서 잰다.**
    압축 고정 바에서 이미 배운 것과 같다: **스크롤한 뒤에만 존재하는 것은 따로 재야 한다.**
    `check-nuclides` ⑥ 이 일곱 폭에서 ① 흔한 폭에 스크롤이 없는가 ② 스크롤이 생기는 폭에서
    끝까지 밀어도 첫 칸이 보이는가를 잰다. 역테스트 둘로 확인했다
    (고정을 떼면 320px 에서, 칸을 다 보이게 하면 360·390·700px 에서 걸린다).
  ★★★ **그리고 그 수리가 다른 게이트를 빨갛게 만들었다 — 규칙이 좁아서였다.**
    `check-sticky` 가 「`position: sticky` 인데 `top` 이 `auto` 면 안 붙는다」로 세고 있었는데,
    그것은 **「붙박이 = 세로로 붙는 것」을 전제한 규칙**이다. 가로로 붙는 것도 있다 —
    목록 표의 첫 칸은 `left: 0` 으로 붙고 `top` 은 `auto` 인 것이 **맞다**. CI 가 148행을
    전부 결함으로 셌다. 지금은 **네 축 중 하나에만 붙으면 통과**하고, 「바가 덮는가」는
    **세로로 붙는 것만** 본다(가로로만 붙은 칸은 본문처럼 흘러가므로 바 밑을 지나는 것이 정상).
    느슨해진 만큼 역테스트로 다시 확인했다 — 어느 축에도 안 붙은 `sticky` 를 심으니 잡는다.
  ★ **내 실수도 함께 적어 둔다**: 표를 고친 뒤 `check-sticky` 를 다시 돌리지 않았다.
    한 게이트를 위해 고친 것이 **다른 게이트의 전제를 깬다** — 쪽을 늘리거나 조판을 바꾸면
    `npm run gate` 전체를 돌릴 것.

## ★★ 이미지 미리보기 허락 — `Base.astro` 한 줄 (2026-09-17)

색인되는 쪽은 `index, follow, **max-image-preview:large**`, `noindex` 쪽은 `noindex,follow` 다.

· 구글이 이 지시가 **없을 때 쓰는 기본값은 「기본 크기」 미리보기**이고, `large` 가 더 큰 것을
  여는 **옵트인**이다. ★★ **허락일 뿐 보장이 아니다** — 어떤 그림을 쓸지, 쓸지 말지는
  구글이 정한다. 우리가 재는 것은 「썸네일이 붙었는가」가 아니라 **「막고 있지 않은가」**다.
· ★ **그전에는 우산 루트에만 있었다.** 이 lab 에는 **robots meta 가 아예 없었다**(실측) —
  `/radimeter/`·`/disposal` 도 마찬가지였다. **같은 오리진 안에서 층마다 다른 말**을 하고
  있었던 셈이다. 계기는 소유주의 「이미지가 안나오는데?」였고, 그 원인 자체는 재크롤 지연
  이었지만 **우리 쪽에 남은 것을 훑다가 이것이 나왔다.**
· ★ **`noindex` 인 쪽에는 주지 않는다** — 색인하지 말라고 한 쪽에 미리보기 크기를 말하는 것은
  뜻이 없고, **섞여 있으면 둘 중 하나가 실수라는 신호**다.
· 실측: 색인 **158쪽** 전부 허락 있음 · `noindex` **1쪽**(`/saved/`) 그대로 · **둘 다 든 쪽 0.**
· 플랫폼 정본은 `radiation-lab` 의 README 이고, 그 레포가 사이트맵의 `lastmod`·이미지 항목과
  게이트(`check-sitemap`)를 든다.

## ★ 검색 제목은 `seoTitle` 이다 — 내비 라벨(`name`)과 갈라 둔다 (2026-09-16)

소유주 지시 「제목 고치자. **사람이 유입이 될 수 있도록** 노력하자.」

실측: 라이브 10쪽 중 제목에 **`calculator` 가 든 쪽이 1개**뿐이었다(허브만).
「Gamma dose rate and shielding」인데 **사람들은 「gamma dose rate calculator」를 친다.**

· ★★ **한 값으로 묶으면 둘 중 하나가 반드시 나빠진다.** 내비·카드는 짧아야 하고
  (줄이 바뀌면 조판이 무너진다) 검색 제목은 사람이 실제로 치는 낱말을 담아야 한다.
  그래서 등록부(`lib/tools.ts`)에 **`seoTitle` 을 따로** 두고 `ToolPage` 가 그것을 쓴다.
  **쪽마다 적지 않는다**는 이 등록부의 원칙은 그대로다.
· **새 주장을 만들지 않았다** — 전부 그 도구의 `blurb` 가 이미 말하는 범위 안이다.
  예: 단위 변환은 「never across」이므로 제목도 **`activity, dose, exposure`** 까지만 말한다
  (`Bq → Sv` 를 해 준다고 읽히면 안 된다).
· OG 그림은 **짧은 `name` 을 계속 쓴다** — 공유 카드는 검색 결과와 **보는 사람이 다르다.**

```
units             Radiation unit converter — activity, dose, exposure
decay             Radioactive decay calculator — half-life and activity
gamma-shielding   Gamma dose rate calculator and shielding
beta              Beta dose rate calculator and shielding
mda               MDA calculator — detection limits and scan MDC
specific-activity Specific activity calculator — mass and activity
alara             ALARA calculator — stay time and collective dose
```

## ★ 구조화 데이터 — 이 lab 10쪽에 0개였다 (2026-09-16)

소유주 질의 「이제는 검색에 유리한 상태야?」 → 라이브 55쪽 전수 감사에서 나왔다.
RadiMeter 는 34쪽이 들고 있는데 **여기는 0개**였다. 검색이 「이게 무엇인지」를 읽는 자리다.

`Base.astro` 에 `WebSite` + `WebApplication` 을 세워 **10쪽 전부가 받는다**(쪽마다 적지 않는다).

· ★★ **없는 것을 주장하지 않는다** — 평점·리뷰·가격을 넣지 않았다. 우리에게 **없기 때문**이고,
  넣으면 구조화 데이터가 화면과 어긋난다(구글의 리치 결과 정책 위반이기도 하다).
  무료라는 것만 `isAccessibleForFree` 로 **사실대로** 적는다.
· 주소는 `canonical()`·`SITE_URL` 에서 파생시킨다 — 글자로 박지 않는다.
· `name` 은 그 쪽의 **`seoTitle`**(검색 제목)을 쓴다 — 검색이 읽는 두 자리가 같은 말을 한다.
· 실측: `/calc/units/` → `WebSite: RadCalc` + `WebApplication: Radiation unit converter — …`,
  주소 리터럴 **0**.

## 유효성 평가 자료 (`/validation/`)

★★ **「우리 도구가 낸 답 = 손으로 계산한 답」을 사람이 읽을 수 있는 형태로 내놓는 자리다**
(2026-09-17 소유주 지시 「사이트 신뢰성 확보를 위해 유효성 평가 자료를 만들어서 추가하려고 해」).
지금 **단위환산 하나**가 평가돼 있고, 나머지 여섯은 허브 쪽이 **없다고 적는다**(없는 것을 주장하지 않는다).

**정본 하나 → 산출물 셋.** 세 곳에 따로 적으면 반드시 한쪽이 낡는다.

```
src/validation/sources.ts      1차 원천 등록부 — 서지·인용문. 여기 없는 값은 케이스에서 못 쓴다
src/validation/units.cases.ts  ★ 케이스 정본 — 단위 44개의 손계산 단계·기대 배수·출처
src/validation/units.test.ts   npm test — 배수 44 + 순서쌍 276 전수 + 합성 12 + 물리 6 + 거부 7
src/pages/validation/units.astro  공개 낱장 — 케이스에서 그린다. 합격 여부·건수·최대 상대차도 **빌드가 잰다**
gate/check-validation.mjs      ①커버리지 ②독립성 ③화면 실측
```

★★ **기대값을 엔진에서 계산하면 순환논증이다.** `units.cases.ts` 는 `engine/` 의 값을
**하나도 import 하지 않는다**(`import type` 만 허용). 배수는 전부 원문에서 사람이 옮겨 적은 수
(BIPM SI Brochure · 12th/15th CGPM 결의 · NIST SP 811 App. B.9 · IAEA TRS-398 · IAEA STI/PUB/1196)
끼리의 산술뿐이고, 게이트 ②가 그 독립성을 지킨다.

★★ **배수 44개면 충분한 이유.** 엔진의 환산은 `value × f_from / f_to` 한 줄이다 — 각 단위의
기준단위 배수를 독립적으로 확인하면 같은 군의 **순서쌍 276개**가 그 비로 따라온다.
그 논증을 말로 두지 않고 **276쌍을 전수 대조**한다(표본이 아니다). 실측 최대 상대차 **1.455e-16**.

★★★ **엔진만 재면 유효성 평가가 엔진에서 멈춘다.** 화면은 답을 반올림해 그리고
(환산표 6자리 · 유도값 칸 4자리 — **자리마다 반올림이 다르다**), 군 전환·표 행 짝짓기는 엔진 바깥이다.
역테스트가 그 이유를 보여 준다 — **환산표의 방향만 뒤집었더니 `npm test` 86건은 전건 통과**하고
게이트 ③이 282건을 잡았다. 게이트는 표 상태 44가지를 돌며 **화면에 그려진 칸 338개**를 읽는다.

★ 게이트는 `tsx` 로 돈다(케이스 정본이 TS 다) — `npm run check-validation`.

### 붕괴 유효성 평가 — 정의가 아닌 것을 대조하는 법

★★ **단위환산과 층이 다르다.** 환산은 정의만으로 서 있어 **완전할** 수 있었지만(단위 44개 전수),
붕괴는 ① **수학**(A = A₀e^(−λt), 정확) ② **측정값**(핵종별 반감기) 둘 위에 서 있다.
두 층을 한 검사에 섞으면 **어느 쪽이 틀렸는지 알 수 없다** — 그래서 케이스 정본도 갈라 둔다.

★★ **대조 상대를 고르는 것이 이 보고서의 핵심 판단이다.** 우리 반감기는 IAEA Livechart(=ENSDF)
에서 온다. 그러면 **NNDC 는 대조 상대가 못 된다** — 같은 ENSDF 를 내주므로 「사본끼리 같다」를
보일 뿐이다. **DDEP**(Decay Data Evaluation Project)는 별개 평가이고 값마다 **표준불확도**를 싣는다.
★ **정식 간행물은 BIPM 쪽이다**(2026-09-18 소유주 제안으로 확인) — **BIPM Monographie-5
『Table of Radionuclides』** 가 그 평가이고, LNE-LNHB 가 낱장으로 최신판을 유지한다
(`lnhb.fr/nuclides/<핵종>_tables.pdf`). **인용은 BIPM 으로, 읽은 자리는 낱장으로** 적는다.

★★ **「몇 % 이내」로 판정하면 안 된다.** Ni-63 은 DDEP 불확도가 **±2.4%** 라 2% 차이가 정상이고,
Ir-192 는 **±0.018%** 라 0.1% 차이면 이상하다. **자를 값마다 그 값의 불확도로 준다**(σ, 한도 3σ).

★ **자동으로 긁은 기준값은 그 자체로 위험하다** — 첫 추출이 **U-238 을 24.10 d** 로 뽑았다.
낱장에 **딸핵종의 T½ 도 함께** 실려 있어서다(24.10 d 는 Th-234). 괄호 안 동위원소를 확인한 줄만
쓰고, **옮긴 원문 줄을 `quoted` 에 함께 실어** 쪽이 그리게 했다.

★★★ **해가 두 가지라는 것이 실제 결함이었다.** 율리우스년 365.25 d 와 그레고리력 평균년
365.2425 d 가 0.002% 다른데, 이 레포가 **둘 다 쓰고 있었다**(`Decay.tsx` 그레고리력 ·
`Beta.tsx` 율리우스년). 정본을 `engine/constants.ts` 의 **`TIME_S`** 한 곳으로 모았다.
· **그레고리력을 고른 것은 취향이 아니다** — IAEA 의 `half_life_sec` 이 그 해로 만들어져 있다.
  두 평가가 **같은 수를 싣는 핵종 넷**(Am-241·Ra-226·C-14·U-238)에서 잔차를 재면
  율리우스년 2.1×10⁻⁵ · 그레고리력 10⁻⁶ 미만이다. **자료를 따라간다.**
· ★★★ **그 검사가 처음에 죽어 있었다.** 역테스트로 화면의 해를 바꿔 보니 **테스트도 게이트도
  안 걸렸다** — 테스트가 **자기 사본**(`365.2425 * 86400`)과 비교하고 있었고, 화면 실측은
  4자리 표시라 0.002% 가 묻혔다. 지금은 테스트가 **도구의 상수를 직접 읽는다.**
  **「무엇과 비교하는가」를 틀리면 검사가 통과한 채로 죽는다.**

★ 화면 실측은 갈래 셋을 실제로 눌러 **읽은 값 12개**를 잰다. 반올림이 또 다르다 —
으뜸 답 4자리 · 검산용 수치 3자리(`fmt(v, 3)`). **자리마다 그 자리의 자를 쓴다.**

★ 커버리지의 뜻도 다르다 — 핵종 147종을 다 대조할 수 없다(DDEP 가 전부를 평가하지도 않았다).
그래서 **① 화면의 갈래 3/3 을 덮었는가 ② 없는 핵종을 평가했다고 주장하지 않는가**를 센다.

### 감마 유효성 평가 — 유도량을 문헌과 맞대면 안 되는 경우

★★★ **출력을 맞대지 않고 ① 유도식과 ② 입력을 각각 검증한다.** 규약이 엇갈리는 유도량을
다루는 옳은 방법이고, 이 도구가 정확히 그 경우다. 근거 셋 —
· **양이 다르다.** 우리는 **공기커마율상수 Γ_δ**, 공개 편찬물(ORNL/RSIC-45)은 **선량당량**률상수다.
  원문 p1 의 식 (1)(2)로 확인했다.
· **환산인자가 유료 규격(ANSI/ANS-6.1.1)의 표**라 넣을 수도 없다(절대규칙 2).
· **δ 를 안 밝힌 값과의 일치는 우연이다** — Am-241 은 δ=10↔20 keV 에서 **×7.84**, Ir-192 ×1.09.

★★ **그전 골든 벡터가 「문헌과 2% 이내」인데 어느 문헌인지가 없었다.** 절대규칙 3 이 「문헌」이라는
낱말 뒤에 숨어 있던 자리다. 지금은 **회귀 고정값**이라고 정직하게 부르고, 진짜 검증은 보고서가 한다.

★★ **DDEP 는 광자 방출확률을 바로 안 싣는다** — 전이확률과 내부전환계수를 싣는다.
**Pγ = Pγ+ce / (1 + αT + απ)** 로 **유도**해야 하고, 그래서 검증으로서 강하다(베끼기가 아니다).
· **내부쌍생성 απ 를 빼면 틀린다** — Co-60 1332 keV 가 0.0034% 어긋났다가, 넣으니 우리 자료와
  **자릿수까지 일치**했다. 우리 자료와 유도가 **둘 다** 맞아야 그렇게 떨어진다.
· 유도가 **스스로를 확인한다** — DDEP 가 따로 권고하는 Cs-137 85.01(20)% 를 유도가 재현한다.
· **읽을 수 없는 낱장은 쓰지 않는다** — Am-241 전이표는 「≈」와 빈칸이 섞여 있어 **뺐다**.
· ★★ **배율이 낱장마다 다르다** — Cs-137 (10⁻¹) · Co-60 (10⁻⁴) · **I-131 은 배율 없는 절대값**
  (164 keV 가 M4 라 αT > 50). 잘못 읽으면 **50배** 틀린다. 그런데 **읽기가 스스로를 확인한다** —
  절대값으로 읽어야 우리 자료와 0.015% 로 맞는다. **자릿수가 맞는 것이 읽기가 맞다는 증거다.**
· ★★★ **BIPM Monographie-5 에는 감마상수가 아예 없다**(Vol.1 311쪽 전수, `kerma`·`Γ`·
  `dose rate constant`·`exposure` **0회**). 국제 계량기구의 핵종표가 **방출까지만** 싣는다 —
  그 뒤(어느 양·어느 δ·어느 가중함수)는 평가자료가 아니라 **응용 규약**이기 때문이다.
  「출력을 맞대지 말고 입력과 유도식을 검증하라」의 가장 강한 방증이다.

★★★ **같은 물리가 두 번 구현돼 있었고, 화면은 보고서가 안 재던 쪽을 쓴다.**
`pointSourceDoseRate`(Γ·A/d²)와 `shieldedDoseRate`(스스로 3.6e15/(4π)를 다시 만든다)가
각자 있는데 **화면은 후자만** 쓴다. 지금 둘은 1.8e-16 로 일치하지만 **그걸 강제하는 것이
아무것도 없었다.** 항등식 `G-ID-10` 이 이제 강제하고, 손계산도 **두 경로 다** 잰다.
· ★ 이건 역테스트가 **엉뚱한 함수를 건드려서** 드러났다 — 게이트가 10% 오차를 못 잡길래 팠더니
  게이트가 아니라 **내 역테스트가 화면이 안 쓰는 함수를 고치고 있었다.**
  **게이트가 안 잡으면 게이트를 의심하기 전에 무엇을 건드렸는지부터 본다.**

★ **보간의 표점 재현은 「정확히 0」이 아니다** — 처음에 그렇게 걸었다가 검사가 내 주장을 잡았다.
`log(y0) + (log(y1) − log(y0))` 가 부동소수로 `log(y1)` 과 같지 않아 **5.8×10⁻¹⁶** 이 남는다.
엔진을 고칠 수도 있지만 어떤 답도 안 바뀌므로 **고치지 않고 사실을 적는다.**

★ **흡수단에서 주석과 코드가 달랐다** — 주석은 「위쪽 가지를 쓴다」인데 실제로는 **흡수단 정확히
그 에너지에서 아래 가지**를 낸다(점프는 1 ulp 위). Γ 영향은 0 이지만 **적힌 것과 하는 것이 다르면
적힌 쪽을 고친다.**

★ **`where` 같은 값은 쪽에 그려진다 — 영문이어야 한다**(절대규칙 5). 한국어로 적었다가
산출물에 샜고 `check-output` 이 잡았다. **주석은 한국어, 값은 영문.**

### 아무 자도 안 보던 두 층 — `astro check` 3013건과 크로미움 경로 열한 곳 (2026-09-20)

**README 가 새 세션에 시키는 절차(3·4단계)가 둘 다 제 일을 못 하고 있었다.** 코드를 고친 것이
아니라 **재는 자를 켠 것**이다 — 산출물은 `dist/` sha256 까지 **한 바이트도 달라지지 않았다**
(고치기 전후를 같은 자로 비교했다).

#### ① `npx astro check` — 오류 **3013건**이 서 있었다

| | 전 | 후 |
|---|---|---|
| `astro check` 오류 | **3013** | **0** (97파일) |
| `/validation/decay/` | 1818 | 0 |
| `/validation/beta/` | 1195 | 0 |
| `npm test` · `tsc` · `npm run build` · 게이트 11 | 전부 초록 | 전부 초록 |
| `dist/` sha256 | — | **같다** |

★★ **원인은 문법 오류가 아니라 스캐너가 `/` 를 정규식으로 읽은 것이다.** 프런트매터의
`c.input.target! / c.input.a0` 에서 `!` 는 TS 의 non-null 단정인데, Astro 의 토크나이저는
**JS 의 논리 부정**으로 읽는다 — 부정 연산자 **뒤**는 정규식이 올 수 있는 자리이므로 `/` 가
**정규식 시작**이 되고, 닫는 `/` 를 찾아 **프런트매터 끝의 `---` 을 삼킨다.**
그 뒤로는 템플릿 전체가 TS 식으로 해석돼 `Cannot find name 'table'` 같은 것이 3천 줄 쏟아진다.
**고치는 법은 괄호 한 쌍** — `(c.input.target!) / c.input.a0`. `)` 뒤의 `/` 는 나눗셈으로 읽힌다.
실제로 걸린 자리는 다섯이고 **정확히 `! /` 라는 모양** 하나였다(`grep -n '! */'` 로 전수).

★★ **왜 아무도 못 봤나 — CI 에 `astro check` 가 없었다.** `tsc --noEmit` 은 `.astro` 를
**한 글자도 보지 않는다**(`npm run typecheck` 에는 둘이 다 들어 있는데 CI 는 `tsc` 만 불렀다).
빌드도 통과한다 — **빌드에 쓰이는 파서는 다른 것**이고, 산출 HTML 은 멀쩡했다.
그래서 **쪽의 타입을 보는 자가 0개**인 층이 생겼고, 그 두 쪽에 진짜 타입 오류가 들어와도
**3013건 속에 묻혀** 아무도 못 봤을 것이다.

★ **지키는 것은 CI 의 「쪽 타입 (astro check)」 단계**다. **역테스트**: 괄호 **한 쌍만**
되돌리면 `astro check` 가 **1818건**으로 죽는다(종료코드 1). 다섯 자리를 다 되돌리면 3013건.

★ **찾아낸 방법을 적어 둔다** — 오류 목록의 첫 줄(`---` 을 가리킨다)은 **결과이고 원인이 아니다**.
`@astrojs/astro2tsx` 의 `convertToTsx` 를 직접 불러 **프런트매터를 한 줄씩 늘려 가며** 진단이
처음 나오는 지점을 찾았다. ★ 그때 **주석·괄호 깊이를 함께 추적해야** 한다 — 안 하면 문장
중간에서 자른 프리픽스가 **가짜로** 걸려 엉뚱한 줄을 가리킨다(두 번 그랬다).

#### ② 크로미움 경로 — 같은 뜻을 **네 가지로** 적은 열한 자리

원격 컨테이너에서 `npm run gate` 가 **둘째 게이트(`check-render`)에서 죽었다**:
playwright 1.63 은 `chromium_headless_shell-1243` 을 찾는데 깔려 있는 것은 **1194** 다.

★★ **죽는 방식이 나빴다** — playwright 가 「`npx playwright install` 을 돌려라」라고 권하고,
그것을 따르면 **브라우저를 새로 내려받는다**(이 환경이 금하는 것이다). 고칠 것은 판이 아니라
**경로를 아는 자리가 열한 곳**이라는 것이었다. 실측한 표기 네 가지 —

| 표기 | 자리 | 미리 깔린 것을 찾는가 |
|---|---|---|
| `PW_CHROMIUM` → `CHROMIUM_PATH` → 자동 탐지 | `check-lockup` 1곳 | **찾는다** |
| `process.env.CHROMIUM_PATH ? {…} : {}` | 게이트 6곳 | 못 찾는다 |
| `{ ...(process.env.CHROMIUM_PATH ? {…} : {}) }` | 게이트 2곳 | 못 찾는다 |
| `const EXE = process.env.CHROMIUM_PATH` | 생성기 2곳 | 못 찾는다 |

지금은 **`gate/chromium.mjs` 한 곳**이고 열한 자리가 `chromium.launch(LAUNCH)` 만 쓴다
(`check-lockup` 의 자동 탐지 로직이 정본이 됐다 — **잘 하고 있던 한 곳을 정본으로 올린 것**이다).
CI 는 `playwright install` 로 기본 경로에 받으므로 `/opt/pw-browsers/chromium` 이 없고,
그때는 `executablePath` 를 주지 않는다 — **CI 동작은 달라지지 않는다.**

★ 지키는 것은 **`gate/check-gates.mjs`** 이고 `npm run gate` 의 **맨 앞**에 둔다(정적 검사라
1초도 안 걸린다). 크로미움을 못 찾는 상태라면 그 사실이 **첫 줄에** 떠야 한다.
playwright 를 쓰는 파일 **11개**를 세고, ① 정본에서 `LAUNCH` 를 임포트하는가 ② 경로 환경변수를
스스로 읽지 않는가 ③ `chromium.launch()` 에 넘기는 것이 `LAUNCH` 인가 를 본다.

★★ **자가 자기를 재어 첫 실행에서 빨개졌다** — 이 게이트의 **정규식 안에** `playwright`·
`CHROMIUM_PATH` 라는 글자가 있다. 자기 자신을 빼야 한다(자매 레포가 적어 둔
「정규식 안의 인용부호가 소스 파싱을 어긋나게 한다」와 같은 층의 함정이다).

★★★ **역테스트 ② 가 처음에 아무것도 증명하지 못했다 — 자를 잘못 골랐기 때문이다.**
임포트를 지웠는데 게이트가 **초록으로 통과했다**: 검사가 `s.includes("chromium.mjs")` 였고
같은 파일 **주석**에 「정본은 `gate/chromium.mjs`」라고 적혀 있었다. **주석이 자를 속인 것이다.**
지금은 문장이 아니라 **임포트문**(`import { … LAUNCH … } from ".../chromium.mjs";`)을 본다.
· 역테스트 ① 옛 표기로 되돌림 → **2건**(파일 이름과 실제로 넘긴 인자를 댄다)
· 역테스트 ② 임포트만 삭제(**주석은 그대로 둔다**) → **1건**. 주석을 남긴 것이 요점이다 —
  주석까지 지운 역테스트는 **고친 자를 시험하지 못한다.**

★ **교훈**: 「있는가」를 문자열 포함으로 묻는 검사는 **주석·문서·역테스트 자신에게 속는다.**
문법 요소를 재려면 문법을 보아야 한다.

### 핵종 낱장의 되풀이 ④ — **제목이 쪽마다 다른 것을 말하게 했다** (2026-09-20)

**제목 틀 겹침 중앙 75.0 → 20.0%** · **147장 전부에 글자까지 같은 제목 4개 → 0개.**
①~④ 를 합치면 산문 되풀이 66.4 → 27.9%, 제목 75.0 → 20.0%.

★★ **이름을 다른 상수로 바꾸는 것은 아무 소용이 없다** — ③단계에서 이미 확인하고 버린 길이다.
재는 자가 핵종 이름을 지운 뒤 비교하므로 「X at a glance」는 147장에서 **같은 문자열**이다.
내려가게 하려면 **제목이 쪽마다 다른 것을 말해야** 한다.

★★ **글을 쓰기 전에 모의로 먼저 쟀다.** 네 제목을 그 쪽이 실제로 가진 것에 따라 가르면
얼마가 되는지 계산해 보니 **75.0 → 20.0%** 였다. 실제로 고친 뒤 값이 **모의와 정확히 일치**했다.
앞 세 단계에서 매번 「해 보니 예상과 달랐다」를 겪었으므로, **손대기 전에 재는 것**을 먼저 한다.

★ **고친 넷** — 제목이 그 쪽에 실제로 있는 것을 말한다(명사형 유지):
· `Key figures` → **표에 실제로 서는 줄**(`Half-life, specific activity and dose rate` 등 4갈래)
· `How the activity falls` → **붕괴 표가 덮는 눈금**(열 반감기 기준 6갈래)
· `What this page does not tell you` → **그 쪽에서 실제로 문제가 되는 한계**(4갈래)
· `Open X in a calculator` → **이어지는 계산기**(4갈래)
훑는 사람에게도 이쪽이 낫다 — **제목만 읽어도 그 쪽에 감마가 있는지 알 수 있다.**

★★ **숫자를 제목에 쓰지 않는다** — 재는 자가 숫자를 `#` 으로 바꾸므로 「5 calculators」와
「3 calculators」가 **같은 문자열로 접힌다.** 세는 말은 낱말로 쓰거나 아예 쓰지 않는다.

★★ **게이트가 세기만 하고 막지는 않고 있었다.** `universal`(147장 전부에 같은 제목)은
이웃 lab 이 애드센스에서 탈락한 **바로 그 모양**인데 **보고만 하고 통과시켰다.**
**재는 것과 막는 것은 다른 층이다** — 이제 하나라도 되살아나면 그 자리에서 막고 **이름을 댄다**.

★★ **눈으로 둘을 잡았다 — 게이트 값은 둘 다 통과한 상태였다**:
① **띠 이름이 그 눈금을 안 가리켰다.** 경계를 100년에 두는 바람에 **H-3(열 반감기 123년)·
   Cs-137(301년)·Sr-90(288년)이 「Activity over millennia」**라고 말했다.
   **이름이 가리키는 눈금이 맞는지 값을 넣어 확인할 것.**
② **제목이 본문과 어긋났다.** 감마를 먼저 보는 바람에 **Pu-239·U-238 이
   「Limits of these dose rates」**가 됐다 — Γ 가 1e-5 도 안 되는 알파 방출체이고
   **그 쪽 본문은 「외부선량이 한계가 되는 일은 드물다」고 말한다.**
   알파가 있고 Γ 가 약한 띠(0.005, 산문과 같은 문턱)면 섭취 쪽을 들게 했다.
   **제목이 본문과 다른 말을 하면 제목이 거짓말을 하는 것이다.**

★ **역테스트 둘**: 네 제목을 옛 상수로 되돌리면 두 검사가 걸리고(75.0% · 공통 4개),
**하나만 되돌려도 걸린다**(27.3% · 공통 1개 — 어느 제목인지 이름까지 댄다).

### 미결 — `check-render` 의 간헐 404 (2026-09-20, **원인 미확정**)

낱장 몇 장에서 「JS 오류 — Failed to load resource: 404」가 **라운드마다 다른 핵종에서** 뜬다.
**같은 빌드(③)가 한 번은 빨갛고 두 번은 초록이었다** — 내용 변경과 무관하다.

★★ **가설 하나를 세우고 실측으로 기각했다.** 라이브에서 `/favicon.ico` 는 오리진 200(우산이
내준다)인데 게이트 서버는 그것을 `dist/calc/favicon.ico` 에서 찾아 404 를 낸다 — `/track.js`
에는 스텁이 있는데 같은 부류인 favicon 은 빠져 있었다. 그럴듯했다. **그런데 60장을 연속으로
돌며 서버로 들어온 요청을 전수로 세니 오리진 루트 요청은 `/track.js` 하나뿐이었고
`/favicon.ico` 는 한 번도 안 불렸다**(콘솔 오류 0건).
★ **고치지 않고 시험한 것이 남는다** — 그대로 스텁을 넣었으면 **원인이 아닌 것을 고쳐 놓고
해결했다고 적었을 것**이다. 게이트 주석이 이미 적어 둔 것의 세 번째 사례다:
「없는 것을 못 본 척」과 「있는 것을 흉내 냄」은 다르고, **있지도 않은 요청을 흉내 내는 것**은
또 다른 종류의 거짓말이다.

★ **지금까지의 사실**: 낱장이 부르는 자원은 HTML·CSS·`favicon.svg`·`/track.js` 넷뿐이고
(외부 주소는 전부 `<a href>` — 공유·출처라 불러오지 않는다), 60장 연속 적재에서는 404 가 0건,
전수(167쪽 × 2뷰포트)에서만 간헐적으로 난다. **쪽의 결함이 아니라 게이트가 부하를 받을 때
생기는 것**으로 보이지만 그것도 **아직 증거가 아니라 다음 가설**이다.

★ **다음에 걸리면 스스로 이름을 댄다** — `check-render` 가 `response`(4xx 이상)와
`requestfailed` 를 함께 듣고 **주소와 상태**를 적도록 고쳤다. 그 메시지 하나가 없어서
진단이 길어졌다: 서버측 404 로그가 0건인데 브라우저는 404 를 본다고 하니, **내 서버로 간
요청이 아니라는 것**조차 메시지만 보고는 알 수 없었다.

### 핵종 낱장의 되풀이 ③ — **문장 틀을 물리적 눈금으로 갈랐다** (2026-09-20)

**되풀이 몫 중앙 48.7 → 27.9%** · 최악 64.0 → **38.8%**(cd-109) · 낱말 겹침 25.1 → **16.6%** ·
거의 같은 쌍 2 → 1. ②단계까지 합치면 **66.4 → 27.9%**.

★★ **자에 맞춰 꾸민 것이 아니라는 확인**: 이번에는 **옛 ④(숫자 포함 5-그램)도 9.9 → 4.4%** 로
함께 내려갔다. ②단계에서는 옛 ④ 가 **전혀 안 움직였는데**(그때는 `prose-doc` 바깥만 고쳤다)
③단계는 본문을 고쳤기 때문이다. **독립적인 옛 자가 같은 방향으로 움직이면** 그것이 근거다.

★★★ **지배항은 생성 산문의 「틀」이었다.** 숫자를 빼고 재면 드러난다 — 세 문단이 147·96·96장에
**글자까지 같은 뼈대**로 나가고 있었다:
· 첫 문단 「… decays by … with a half-life of … / A gram of it holds … / After … is left, and after …」
· Γ 문단 「Γ comes out at …, which is …× Cs-137 …, and ranks N of the 96 photon emitters …」
· 차폐 문단 「Halving the air kerma rate takes X of lead or Y of steel; a tenth takes Z …」

★★ **띠는 물리적 눈금으로 가른다 — 그리고 어느 띠도 절반을 넘지 않아야 한다.**
게이트가 세는 것이 「절반 이상의 쪽에 나오는 5어절」이므로, 띠 하나가 절반을 넘으면 **그 띠가
통째로 되풀이로 세어져 헛일**이다. 실측으로 구획을 골랐다 —
· 비방사능 `[1e9·1e12·1e14·1e16]` Bq/g → **17·18·25·40·47장**(절반 74)
· Γ `[0.005·0.05·0.2]` → **21·34·32·9장**(절반 48)
· 납 HVL `[0.2·1·5]` mm → **31·18·22·25장**(절반 48). 처음 잡은 `[1·5·15]` 는 첫 띠가 **49장**으로
  절반을 한 장 넘겨 버렸다 — **구획을 눈대중으로 정하면 안 된다.**

★ **숫자를 피하려고 나눈 것이 아니다.** 납 반가층 0.12 mm 인 쪽과 30 mm 인 쪽은 **같은 공학
문제가 아니다**(한쪽은 용기 벽으로 끝나고 한쪽은 차폐 자체의 무게가 문제가 된다).
6시간짜리와 10억년짜리에 같은 말을 하는 것이 애초에 틀린 글이었다.
★ **띠마다 숫자는 한 개도 빼지 않는다** — 값을 줄이면 되풀이 대신 빈약이 되고 `check-output` ⑥
(어수 250 하한)이 그것을 잡는다. 띠가 바꾸는 것은 **무엇을 앞세우고 어떤 순서로 말하는가**다.

★★ **게이트가 못 보는 결함 다섯을 눈으로 잡았다**(전부 빌드·타입·게이트를 통과한다):
① **순위 방향을 거꾸로 썼다** — `gammaRank` 는 **센 쪽부터**인데 「{rank}th weakest」라고 적었다
   (Pu-239 는 96 중 90위 = 약한 쪽에서 7번째). **문장이 틀린 사실을 말하고 있었다.**
② **논평을 계산으로 바꿨다** — 「bulk 로 다뤄야 외부선량이 문제가 된다」를 **「20 µSv/h 에 닿는
   활동도」**로 갈았다(Pu-239 2.43 TBq · Am-241 5.41 GBq · Co-60 65.4 MBq). 재는 것이 낫다.
③ **U-238 이 「100.00% … 100.00%」라고 말했다** — 같은 값이 두 번 나오면 그 문장에는 값이 없다.
   **1% 를 잃는 데 걸리는 시간**으로 갈았다(U-238 6480만년 · Th-232 2억년).
④ **눈금 위 경계가 없는 자리 넷** — `act()` 신설(「6.54e+4 kBq」→ 65.4 MBq) · `mass()` 에 `t`·`fg`·`ag`
   (「1.19e+3 kg」→ 1.19 t, U-238m 0.160 ag) · `fmtTime` 에 `billion`·`trillion` 과 **연 자릿점**
   (「4.33e+3 years」→ 4,330 years). **같은 함정을 질량·활동도·시간에서 세 번 밟았다** —
   눈금을 만들 때는 **위 경계를 반드시 준다.**
⑤ 전수 훑기로 확인했다 — 「지수 표기 + 사람이 읽는 단위」가 147장에서 **397건 → 0건**.
   (`sig()` 가 내는 `2.68e+12 Bq` 같은 과학표기는 **일부러 남긴다** — 그것은 물리량이다.)

★ **역테스트**: 차폐 문단 4띠를 옛 단일 틀로 되돌리면 세 검사가 걸린다
(되풀이 33.6% > 30% · cs-137 46.8% > 42% · 낱말 19.0% > 19%).

★ **남은 것은 제목 틀 75.0%** — 147장에 글자까지 같은 제목 넷. **제목 이름을 바꿔서는 안 내려간다**
(핵종 이름을 지운 뒤 비교하므로 상수 제목은 어느 것으로 바꿔도 같은 문자열이다).
내리려면 **제목이 쪽마다 다른 것을 말해야** 한다.

### 핵종 낱장의 되풀이 ② — **되풀이를 걷고 그 핵종의 숫자로 갈았다** (2026-09-20)

실측 결과(같은 자, 전후) — **되풀이 몫 중앙 66.4 → 48.7%** · 최악 80.6 → 64.0%(zr-95) ·
낱말 겹침 31.9 → 25.1% · 거의 같은 쌍 6 → 2. **옛 ④ 는 전 구간에서 9.9% 로 꿈쩍도 안 했다.**

★★ **자를 먼저 한 번 더 고쳐야 했다 — ④-2 에도 같은 사각이 있었다.**
`proseAll` 이 `<p>` 만 세는 바람에 147장의 「What this page does not tell you」 **목록(`<li>`)을
한 번도 안 봤다.** ④ 를 흉본 자리에서 똑같은 것을 밟았다.
· `<li>` 를 통째로 넣으면 **링크 라벨**(「Decay and half-life」)까지 세어져 게이트가 영영 빨갛다.
· 실측으로 갈랐다 — 낱장 `<li>` 의 어수 분포는 **3·5어(라벨 572건)와 25~38어(산문 502건)** 뿐이고
  **그 사이가 완전히 비어 있다.** 12어는 그 20어짜리 빈 골짜기 한가운데다(경계 항목 0건).

★★ **고친 것은 여섯 자리이고, 원칙은 하나다 — 낱장에는 그 핵종의 숫자만 둔다.**
① 광자선 캡션(96장) ② HVL 캡션(96장) ③ TVL/HVL 주석(96장) ④ 베타 캡션(35장)
⑤ 한계 목록(147장) ⑥ 출처 문단(147장). 일반론은 **허브(`/nuclides/`)의 앵커 다섯**으로 보냈다.
**지운 것이 아니라 옮긴 것이다** — 한계 목록 마지막 줄이 전문으로 데려간다(절대규칙 4 의 방어선).

★★★ **분량 하한을 되풀이로 채우고 있었다.** 걷어내자 **18장이 `check-output` ⑥(250어) 아래로
떨어졌다.** **하한을 내리지 않았다** — 되풀이로 채운 분량은 분량이 아니다. 대신 그 쪽들이
**쓰지 않고 있던 자기 자료**로 채웠다(알파 캡션 33장 · 붕괴 캡션 147장 · 컷오프 아래·기록 없는
핵종의 문단 보강). 지금 최소 어수는 **271** 로 깎기 **전보다도 두껍다.**

★★★ **분량을 채우면 되풀이가 되오른다 — 틀로 채우면 그렇다.** 붕괴 캡션을 147장에 한 문장으로
넣자 52.8 → **55.3%** 로 되올랐다. 게이트가 세는 것은 「**절반 이상의 쪽에 나오는 5어절**」이므로
**반감기 자릿수로 띠를 넷으로 갈랐다**(36·48·34·29장, 절반은 74장 — 어느 띠도 안 넘는다).
숫자를 피하려고 나눈 것이 아니다 — **6시간짜리와 10억년짜리에 같은 말을 하는 것이 애초에 틀린
글**이다. 갈고 나니 **48.7%**.

★ **게이트가 못 보는 결함을 눈으로 하나 잡았다** — 이 파일의 `mm()` 은 **표 칸용**이라 단위를
안 붙인다(열 머리글이 든다). 그것을 산문에 써서 「**10.3 of lead halves this spectrum**」이
나갔다. 빌드·타입·게이트 전부 통과했고 **화면의 글을 읽어야만** 보였다. `mmText()` 를 따로 뒀다.

★ **제목은 손대지 않았다** — 상수 제목을 다른 상수 제목으로 바꾸는 것은 이 자에 **아무 영향이
없다**(핵종 이름을 지운 뒤 비교하므로 「X at a glance」는 147장에서 같은 문자열이다).
제목 틀 75.0% 는 그대로이고, 그것을 낮추려면 **제목 자체가 쪽마다 다른 것을 말해야** 한다.

★ **남은 지배항은 생성 산문이다** — `nuclide-prose.ts` 가 만드는 131개 5어절이 **45.8%** 되풀이고,
이제 전체의 절반이 그것이다. 문장 **틀**이 147장에서 같기 때문이고(숫자를 빼고 재므로 그대로
드러난다), 그것을 갈라 내는 것이 다음 단계다.

### 핵종 낱장의 되풀이 — **게이트가 절반만 보고 있었다** (2026-09-20)

소유주 질의 「애드센스 신청하면 가치 있는 사이트로 나올까」에서 라이브 211쪽을 전수로 재다
나왔다. 결론부터 — **콘텐츠·분량·정책문서·내비는 통과 수준이고, 실질 위험은 핵종 낱장 147장
하나**다(전체 쪽의 70%). 이웃 lab 이 애드센스에서 탈락한 원인이 바로 「장 제목이 글자까지 같고
본문만 다른」 묶음이었다.

★★★ **`check-nuclides` ④ 가 시야의 절반을 놓치고 있었다.** 두 가지다.
· **자리** — ④ 는 `<section class="prose-doc">` **안만** 본다. 그런데 각 구획의 설명 문단
  (「Emission probability is how often a line appears…」 · 「Solved for this nuclide's whole photon
  spectrum…」 · 「What this page does not tell you」)은 **그 바깥**에 있다.
  prose-doc 156어 · 본문 산문 전체 326어 — **절반이 안 보였다.**
· **자** — ④ 의 `grams()` 가 `[^a-z0-9 ]` 라 **숫자를 남긴다.** 핵종마다 숫자가 다르니 겹침이
  씻겨 내려간다. **심사자는 숫자가 아니라 문장을 읽는다.**
  같은 쪽을 두 자로 재면 **숫자 포함 9.9% · 숫자 제외 31.9%**.

★★ **④ 를 고치지 않고 ④-2 를 나란히 세웠다.** ④ 는 「자료가 실제로 다른가」를 묻고
④-2 는 「설명을 되풀이하는가」를 묻는다 — **다른 질문이다.**

★★★ **머릿수는 「되풀이 몫」**이다 — 그 쪽 산문 중 **절반 이상의 다른 낱장에도 나오는 몫**.
쌍끼리의 겹침보다 읽는 사람의 경험에 가깝다(한 장을 열었을 때 몇 할이 남의 쪽에서 본 문장인가).
**2026-09-20 실측 중앙 66.4% · 최악 80.6%(zr-95)**. 구획 수별로는 **6구획 쪽이 76.1%** 로 가장
심하다 — 감마·차폐 설명 문단 두 개가 거기 있다.

★ 상한은 **래칫**이다 — 지금 값에 맞춰 **나빠지는 것만** 막고, 허브로 옮긴 뒤 함께 내린다.
**고치기 전 값을 박아 두는 것**이 요점이다.

★★★ **역테스트를 두 번 했고, 첫 번째는 아무것도 증명하지 못했다.**
되풀이 문단을 `prose-doc` **안**에 넣었더니 ④ 도 움직여(9.9→24.9%) ④ 의 사각이 안 보였다.
**바깥**에 넣고서야 갈렸다 — **④ 는 9.9% 에서 꿈쩍도 않고**, ④-2 는 네 검사가 전부 걸렸다
(되풀이 71.1% · 낱말 44.6% · 거의 같은 쌍 10). **엉뚱한 자리를 건드린 역테스트는 게이트가
멀쩡하다는 뜻이 아니다** — 감마 라운드에서 같은 함정을 밟았다.

### 작업계획(ALARA) 유효성 평가 — **대조할 자료가 없다는 것이 성격이다** (2026-09-19)

★★★ **이 도구는 핵자료도 경험식도 규격 상수도 쓰지 않는다.** 역제곱은 기하, 체류시간은
나눗셈, 집단선량은 합, 반가층은 정의다. 그러니 「MARSSIM 의 414 를 맞혔다」 같은 말을 할 수 없다.
**그것을 만들어 내지 않고 그대로 적는 것**이 이 보고서의 첫 문단이다.

★★★ **그러면 무엇이 남는가 — 자료가 없는 도구에서 틀릴 수 있는 것은 둘이다.**
① **가정이 어디서 깨지는가** ② **답이 없을 때 답하는가**. 찾은 결함 아홉이 **전부 ②에 있었다.**

★★★ **「점선원을 가정한다」를 거리 기준으로 바꿨다.** 그 문장은 실무에서 쓸모가 없다 —
「1 m 용접부에서 3 m 면 충분한가」가 답이어야 한다. 선선원의 해석해가 정확히 알려져 있으므로
**바깥 자료 없이 계산된다**: 비 = arctan(x)/x, x = L/2d.
· d=L **7.84% 과대평가** · d=3L **0.92%** · d=10L **0.083%**
· 1% 이내로 두려면 **d ≥ 2.88 L**, 0.1% 이내면 **d ≥ 9.13 L**
· ★ **언제나 과대평가한다 = 안전한 방향**이다. 계획이 보수적이 된다는 뜻이고,
  이것도 도구의 주의문에는 없던 말이다. **주의문보다 숫자가 쓸모 있다.**

★★★ **이 라운드의 가장 중요한 발견 — 같은 결함을 절반만 고쳤던 자리.**
2026-09-14 에 `stayTime` 이 **선량률 NaN 에서 「unlimited」**를 내는 것을 고쳤다
(`NaN > 0` 이 false 라 무제한 가지로 떨어졌다). 그런데 **음수도 같은 가지로 떨어진다** —
`-1 > 0` 역시 false 다. **닷새 뒤까지 음의 선량률이 「무제한 체류 가능」**이었다.
> **한 가지 나쁜 입력을 막을 때는 같은 분기로 떨어지는 나머지를 함께 센다.**
지금은 셋을 가른다 — **정확히 0 일 때만 무제한**, 음수·NaN·무한대는 **답 없음**, 그 외는 나눗셈.

★★ **나머지 거부 여덟도 전부 수를 내고 있었다.**
음의 선량률이 **−2.5 mSv/h**, 음의 기준거리가 **−22.4 m**, 음의 예산이 **−20 h**,
음의 작업자 수가 **−6 person·mSv**(여러 줄 합계 안에서 **다른 줄을 조용히 상쇄한다**),
μ=0 이 **무한 반가층**(「아주 두꺼우면 된다」로 읽히지만 실제 뜻은 **그 물질로는 안 된다**).
★ 특히 `distanceForRate(0, 1, 0.5) = 0` 은 **뜻이 정반대**였다 — 「선원에 붙어 서라」로 읽히는데
실제는 **제약이 없어 어느 거리든 된다**는 뜻이다. **정반대를 말하는 수는 없는 것보다 나쁘다.**

### 검출하한 유효성 평가 — **남이 인쇄한 숫자를 맞히는 것이 가장 강하다** (2026-09-19)

★★★ **이번 대조 상대는 아주 좋다.** MARSSIM(NUREG-1575 Rev 1)은 미국 NRC·EPA·DOE 가 무상
공개한 확정판이고 **완결된 수치 예제를 본문에 싣는다.** 유도 검증은 「대수가 맞는가」만 묻지만,
공표된 예제 재현은 **대수가 맞고 답이 틀린 경우**를 잡는다.
· M-P-01 1단계 MDCR **414 cpm** — 자리까지 일치
· M-P-02 2단계 MDCR **372 cpm** — 자리까지 일치
· M-P-03 Tc-99 scan MDC — 우리 **755.9**, MARSSIM **750**. ★ **차이는 MARSSIM 자신의 반올림**이다
  (표 6.6 이 MDCR 을 130 으로 인쇄한다. 130 을 넣으면 750.6). **「대충 맞는다」로 넘기지 않고
  잔차의 출처를 케이스가 적고, 게이트가 그 칸이 비면 실패시킨다.**

★ **판을 가린다.** Rev 2 는 초안이고 쪽마다 「DRAFT — **DO NOT CITE OR QUOTE**」가 찍혀 있다.
같은 문장이 들어 있어도 **쓰지 않는다.** 인용은 Rev 1 에서만 했다.

★★★ **찾은 것 ① — 화면이 d′ 의 뜻을 틀리게 적고 있었다.**
도움말이 「1.38 = 95% 참양성 · **25%** 거짓양성」이라고 했는데, MARSSIM 표 6.5 에서 그 칸은
**60%** 다(25% 칸은 **2.32**). 숫자 하나가 아니라 **방법의 설계**를 오해하게 만든다 —
1단계 스캔은 **거짓양성을 일부러 많이 허용해** 싸게 표시하고, 2단계(조사원이 멈춰 4초 세는
단계, d′=2.48)가 걸러 내는 구조다. 「25%」라고 적으면 1단계가 훨씬 까다로운 것처럼 보이고
**2단계가 왜 있는지가 사라진다.**

★★ **찾은 것 ② — MARSSIM 은 L_D 의 상수를 3 으로 쓴다**(Currie 의 2.71 대신, Brodsky 1992).
우리는 **k 를 사용자가 고르므로**(95/97.5/99%) k² 를 유지한다 — 「3」은 k=1.645 전용이다.
**주장하지 않고 쟀다**: 차는 **0.293975 계수**로 배경과 무관하고, 상대차는
**B=0 에서 10.86% · B=100 에서 0.597% · B=10⁴ 에서 0.063%** 다.
★ **MARSSIM 쪽이 더 크다 = 더 보수적**이므로, 우리는 검출하한을 **조금 낮게** 낸다.
그 **방향**까지 쪽이 적는다 — MARSSIM 을 따르는 사용자가 알아야 할 것은 크기가 아니라 방향이다.

★★ **거부가 여섯 자리 뚫려 있었다.** 화면의 세 효율(검출·표면·관찰자)은 전부 **분율**인데
아무도 1 을 넘지 못하게 막지 않았다 — **관찰자 효율 0 이 Infinity** 를, **효율 1.5 가 답**을
냈다. k 가 음수면 **음의 임계수준**(−23.26 계수)이 나왔다.

★ **`check-og` 가 헛돌 뻔했다 — 같은 파일을 읽어도 읽는 법이 다르면 다른 값이다.**
제목에 아포스트로피가 들어간 쪽이 처음 생기자, 산출 HTML 은 `&#39;` 로 이스케이프하는데
`make-og` 는 그것을 풀어 매니페스트에 넣고 `check-og` 는 안 풀어 **「제목이 바뀌었다」가
영영 떴다.** 제목을 피하지 않고 **비교하는 두 자리가 같은 정규화를 쓰게** 고쳤다
(역테스트로 진짜 제목 변경은 여전히 잡는 것을 확인했다). **늘 빨간 게이트는 없는 게이트다.**

★ **√t 항등식은 점근값이다 — 자를 하나로 쓰면 안 된다.** 비는 2 를 **넘고** 배경이 커질수록
**위에서** 2 로 내려온다(k² 는 1/t, 나머지는 1/√t 로 줄어 짧은 쪽에 더 무겁게 걸린다).
· 케이스 첫 판에 방향을 **거꾸로** 적었다가 테스트가 잡았다.
· 쪽에서도 하나의 자로 재다가 **「One or more checks fail」을 띄웠고**, **지난 라운드에 세운
  게이트 ①-5 가 그 자리에서 잡았다.** 게이트가 바로 다음 라운드에서 값을 했다.

### 베타 유효성 평가 — **경험식은 「맞는가」를 물을 정본이 없다** (2026-09-19)

★★★ **이 보고서는 앞의 넷과 성격이 다르고, 그 차이를 쪽이 먼저 설명한다.**
단위환산은 **정의** · 붕괴/비방사능은 **평가된 측정값** · 감마는 **유도량**이었다.
베타는 **경험식** 위에 선다 — 누군가 측정점에 맞춘 곡선이고, 「참값」을 아는 곳이 없다.
그래서 물을 수 있는 것이 셋뿐이다: ① 구현이 **공표된 꼴**과 같은가 ② **적합 범위**를 지키는가
③ 독립한 근대 자료와 **같은 양으로** 견주면 어떤 관계인가.

★★★ **「세우지 못한 것」을 표로 든다(5건).** 경험식은 정확도를 주장할 수 없으므로 이 표가
보고서의 절반이다. 게이트가 **목록이 비면 실패**시킨다 — 비면 보고서가 스스로를 과장하는
쪽으로 조용히 바뀐다.

★★ **엔진 결함 넷을 찾았다.**
1. **Katz–Penfold 가지 경계가 3 MeV** 였다(공표된 꼴은 2.5). 자료에서 걸리는 것은 **Pr-144
   하나**이고 어긋남 0.687% — 산포 안이지만 **공표된 식과 다른 식을 쓰며 그 이름을 달고 있었다.**
2. **적용범위 하한 가드가 없었다** — `betaRange(0.001)` 이 수를 냈다.
3. ★★★ **NaN 과의 비교는 언제나 false 다.** `thicknessGcm2 >= betaRange(E)` 가 비정이 NaN 일 때
   「두께가 비정보다 작다」로 읽혀 지수식으로 빠졌고, **6.4×10⁻¹⁹⁵ 이라는 수**가 나왔다.
   **막는 조건을 통과했다고 해서 답이 있는 것이 아니다.** 이 레포에서 처음 보는 유형이다.
4. 흡수계수도 같은 구멍(`betaMassAbsorption(0.001)` = 44 715 cm²/g).

★★★ **ESTAR 를 쓰되 양을 섞지 않았다 — 감마 Γ 에서 배운 것이다.**
· **우회인자**: 우리 비정 ÷ NIST CSDA = **0.454–0.794**(알루미늄 39점). NIST 가 직접 정의한다 —
  CSDA 는 **경로길이**, 투사비정은 **깊이**, 그 비는 **항상 1보다 작다.** 이 차이를 「25% 오차」로
  적었으면 보고서 전체가 틀린 말을 했을 것이다.
· **제동복사**: 절대값은 대조하지 않는다(ESTAR 는 단일에너지, 우리 식은 베타 스펙트럼).
  **Z 의존성**만 본다 — 물/아크릴은 ESTAR 1.11–1.13 vs 식 1.138 로 맞고,
  **납/아크릴은 ESTAR 14.7–24.2 vs 식 12.6** 이다. **식이 납의 불리함을 낮춰 말한다** —
  도구의 조언(저Z 를 먼저)은 오히려 **강해지고**, 절대값을 믿는 사용자는 과소평가한다.

★★ **자료는 내부 정합만 잰다**(바깥 평가를 못 들였다 — 목록 B-N-05).
· `beta_mean_keV` 가 **가지 강도가중 평균**임을 35종 전수 확인(최대 1.04×10⁻³).
· ★ **끝점이 희귀 가지인 핵종 둘을 이름으로 밝힌다** — **Co-60(0.120%)** · I-131(0.390%).
  Co-60 의 차폐두께는 **800번에 한 번 나오는 가지**의 것이다. 보수적이라 틀리지 않았지만,
  **흔한 베타를 잰 값이 아니다** — 밝히지 않으면 읽는 사람이 측정과 비교하다 혼란에 빠진다.

★ **출처 없는 주장 둘을 걷었다** — 「0.01 to 3 MeV」와 「accurate to roughly 10%」.
검색 요약 둘이 「2.5 MeV · 평균편차 4.1%」로 일치하지만 **내가 연 문서가 아니라 근거로 쓰지 않는다**
(APS 유료·차단). 경계점 2.5 의 유일한 내부 증거는 **두 가지의 간격이 최소인 점 2.583 MeV**이고,
그것은 **부합하는 증거이지 증명이 아니다** — 쪽이 그렇게 적는다.
★ `17/E^1.14` 흡수계수는 **출처를 못 찾았다.** 사이트 어디에도 인용이 없었고, 지금은
**「출처 없음」으로 표에 적는다.** 있지도 않은 근거를 주장하는 것보다 낫다.

★ 역테스트 넷 전부 걸렸다 — 목록 비우기 · 「roughly 10%」 되살리기 · 경계점 3 으로 되돌리기 ·
NaN 가드 되돌리기.

### ★★★ 보고서가 스스로 실패를 선언한 채 배포돼 있었다 (2026-09-19)

감마 보고서가 라이브에서 **「One or more checks fail — see the tables below」** 를 띄운 채
한 라운드를 서 있었다. 원인은 단순하다 — 감마 라운드에서 항등식 **`G-ID-10`**(같은 물리의 두
구현이 일치하는가)을 케이스 정본에 늘리면서 **쪽의 실행 표(`idRun`)에 잇는 것을 빠뜨렸다.**
`relValue(undefined, 1)` 이 NaN 이 되고 `allPass` 가 false 로 떨어진 것이다.

★★ **`npm test` 는 구조적으로 못 잡는다.** 테스트에는 그 케이스의 실행부가 **제대로 있었다**
(그래서 43건이 전부 통과했다). 빠진 것은 **쪽**이고, **쪽은 테스트가 보는 자리가 아니다.**
엔진·케이스·쪽 셋 중 하나만 빠져도 되는 구조인데, 검사가 둘만 보고 있었다.

★★ **사람도 놓쳤다 — 그게 더 중요하다.** 배포 확인에서 「검사 654건」과 「한국어 0자」는
읽고 **판정 줄은 안 봤다.** 세는 것은 봤는데 **결론을 안 봤다.**
그래서 **사람이 읽어야 했던 자리를 게이트가 읽게 했다** — `check-validation.mjs` **①-5**:
공개된 보고서의 산출 HTML에 **「All checks pass」 가 없으면 실패**.

★ **「NaN 이 있는가」로는 못 잰다** — 거부 표가 NaN 을 **일부러** 그린다(단위 10 · 붕괴 6 ·
감마 2). 보고서에서 신호가 되는 것은 **판정 줄 하나**다.

★ 함께 넣은 id 대조는 **약하다는 것을 실측으로 확인했다** — G-ID-10 은 *그려지긴 했고 값만*
NaN 이라 통과했고, 표에서 `{c.id}` 를 지워도 같은 id 가 다른 자리에 남아 통과한다.
잡는 것은 **케이스를 렌더에서 통째로 뺀 경우** 하나다. **자가 무엇을 못 재는지 적어 둔다.**
· 부수 소득: `D-YR-01`·`G-EM-00` 이 **실행은 되는데 사례번호가 쪽에 안 찍히고** 있었다(찍게 했다).
· 부수 수정: 통과 로그가 「공개 4건 **전부** 통과」라고 박혀 있어 **하나가 실패해도 그대로
  나왔다.** 지금은 **센 것**을 찍는다(「4건 중 3건」). **거짓말하는 로그는 없는 로그보다 나쁘다.**

### 비방사능 유효성 평가 — **일관성 검사는 정확성 검사가 아니다**

★★★ **이 라운드는 결함을 찾아냈다.** 비방사능을 `a = ln2·N_A/(T½·A)` 로 계산하며 **질량수를
몰 질량으로** 쓰고 있었고, 화면·방법론·도구목록·낱장 **다섯 자리**가 그 근사를 「모든 핵종에서
0.03% 이내」라고 적고 있었다. AME2020 과 전수 대조하니 **147 중 95 가 그 한계를 넘었고**
최악은 H-3 의 **+0.535%** — **주장의 18배**다. 0.03% 라는 수의 출처는 끝내 못 찾았다.
· **오차는 반올림이 아니라 결합에너지다.** 원자질량 = A + Δ/c² 이고 Δ 는 A≈56 에서 가장 음이라
  −0.11%, 가벼운 핵에서 크게 양이라 +0.53% 다. **부호가 바뀌는 양에는 작은 상한이 없다.**
· **근사의 한계를 다시 적지 않고 근사를 없앴다** — `scripts/masses.py` 가 AME2020 원자질량과
  NUBASE2020 들뜬에너지를 뽑아 `m_u` 를 넣는다. **비방사능 147개가 전부 바뀌었다.**
· ★ **방향은 정의가 정한다 — 문헌으로 정하지 않는다.** 핵종의 몰 질량은 **원자질량을 g/mol 로
  쓴 것**이고 질량수는 핵자 수다. 다만 이 레포가 그전부터 들고 있던 회귀 고정값 3.56e14 가
  원자질량 쪽(3.5599e14)과 **0.004%**, 질량수 쪽(3.5789e14)과 0.53% 로 갈린다 —
  **출처 없는 값이라 근거로 쓰지 않고**, 변경 **전에** 박혀 있어 맞춰 넣을 수 없었다는 뜻으로만 적는다.

★★★ **왜 오래 안 보였나 — 게이트가 스스로를 검증하고 있었다.** `data.test.ts` 가
「저장된 비방사능이 반감기·**질량수**와 어긋나지 않는다」를 재고 있었다. 데이터도 테스트도
같은 식이니 **영원히 통과한다.** 값을 만든 방식으로 값을 다시 계산하는 검사는 그 값이
무엇이든 통과한다 — **바깥 자료를 들여야만** 틀린 것이 보인다. 그것이 유효성 평가다.

★★ **독립한 둘째 평가가 없으면 없다고 적는다.** 반감기는 ENSDF·DDEP 두 평가가 있어 진짜
대조가 됐지만, 원자질량은 **AME2020 하나**가 사실상 유일한 전지구 최소자승 조정이다.
「두 출처가 맞는다」를 주장할 수 없으므로 할 수 있는 셋을 한다 —
· **유도를 전부 보인다**(몰·베크렐의 정의에서 나온다).
· **평가 안의 독립한 두 열을 맞댄다** — AME2020 은 원자질량과 질량결손을 따로 싣고
  `m = A + Δ/c²` 를 만족해야 한다. 147종 최대 잔차 **5.72e-11**. 평가의 오류는 못 잡지만
  **읽기의 오류**는 잡는다 — 그리고 이 레포가 실제로 밟은 것이 읽기의 오류였다(U-238 사고).
· **원문 줄 147개를 그대로 싣는다** — 독자가 글자로 대조한다.

★★ **`u↔keV` 를 기억으로 적지 않고 SI 정의값에서 유도했다**(이성질체 보정에 필요하다).
`m_u c²/e = M_u c²/(N_A·e)` 이고 c·e·N_A 는 전부 정의값, M_u 만 측정값이다.
NIST 표값 931.494 103 72 MeV 를 **3.1e-12** 로 재현한다 — 인쇄 자릿수의 반올림이다.

★ **이성질체 36종은 들뜬 에너지만큼 무겁다.** 최대가 U-238m 의 2557.9 keV = 상대 1.2e-5 로,
고친 오차의 400분의 1이지만 **AME 불확도(≤1.4e-7)보다는 크므로** 넣는다.

★★ **거부가 다섯 자리 뚫려 있었다** — T½=0·M=0 이 **Infinity** 를, 음의 활성도가 **음의 질량**을
냈다. 전부 화면에서 수처럼 보인다. **「맞는 답이 맞는가」만 묻는 평가는 이런 자리를 영원히
못 본다.** 지금은 붕괴 쪽 관행대로 NaN 을 낸다.

★★ **화면 실측은 으뜸 답만 재면 안 된다.** 몰 질량을 질량수로 되돌려도 답은 0.5% 만 움직여
**4자리 표시에서 정상으로 보인다.** 그래서 게이트 ⑥이 **몰 질량 줄 자체**를 읽고 질량수와
같은지까지 본다(그전 화면은 그 자리에 「Mass number 60」을 그리고 있었다 — 값이 보이는데
틀린 값이었다).
· ★ **이 화면의 아래 표는 커밋된 스냅숏에서 그려진다** — 게이트 첫 판이 계산 단추를 안 눌러
  **19종 전부 Pu-239 의 몰 질량을 읽고 있었다.** 게이트가 기대값과 대조하지 않았다면
  「19종을 돌았다」는 로그만 남고 통과했을 것이다. **센 것이 아니라 읽은 값을 대조할 것.**

### 도구 쪽 세 칸 — 역할이 글로 서 있어야 한다

★★ 2026-09-18 소유주 지적 「1단열 제목이 calculators … **제목처럼 보이게 서식을 입혀줘** …
**오른쪽은 제목이 없네** … 3단의 역할을 분명히 알 수 있게」. 실측 원인이 둘이었다 —
① 왼쪽 **칸 제목** `Calculators` 와 오른쪽 칸 **안**의 소제목 `What this assumes` 가
**같은 `.label`** 이라 층이 하나도 안 갈렸다 ② 오른쪽 칸에는 **보이는 제목이 아예 없었다**
(`aria-label="Reference"` 만 있어 화면에 안 나온다).

· 정본은 `global.css` 의 **`.col-head`** 한 줄이고 두 레일이 함께 읽는다.
· ★ **앰버로 강조할 수 없다** — 규약상 활성·포커스·기준선 전용이고 왼쪽 목록의 활성 칸이 이미
  앰버다. 남은 축으로 낸다 — **명도**(ink-faint → ink) · **무게**(500 → 600) · **괘선**(아래 실선).
· 오른쪽 칸 이름은 **`About this calculator`** 이고 `aria-label` 도 **같은 문자열**이다 —
  보이는 이름과 읽어 주는 이름이 다르면 둘이 따로 낡는다.
· 세 칸은 이제 `CALCULATORS`(어디로 가나) · 도구 이름 h1(무엇을 계산하나) ·
  `ABOUT THIS CALCULATOR`(답이 어디에 서 있나)로 읽힌다.

★ `check-render` 가 셋을 잰다(역테스트 셋으로 확인) — ⓐ **붙박이 레일마다** 보이는 칸 제목이
있는가(공유 줄 같은 `aside` 는 붙박이가 아니라 안 걸린다) ⓑ 칸 제목이 소제목과 **실제로 달라
보이는가**(명도·무게) ⓒ 보이는 이름과 보조기기가 읽는 이름이 같은가.

### 표 조판 — 대조표는 눈으로 대조하는 물건이다

★★ 2026-09-17 소유주가 **휴대폰 화면에서** 잡았다(「표는 신경써서 만들자. 표 열넓이」).
`1.85 × 10⁵` 가 `×` 앞뒤 공백 때문에 좁은 칸에서 **세 줄로 쪼개졌다** — 값 하나가 여러 줄이
되면 그것은 표가 아니다. 실측으로 규칙 넷이 나왔고 전부 `check-render` 가 지킨다(160쪽 × 두 뷰포트).

1. **수 표기에 공백을 두지 않는다** — `sci()` 가 `1.85×10⁵` 로 낸다. 칸에 `nowrap` 을 거는 것보다
   **쪼개질 자리를 없애는 쪽**이 맞다(값이 표 밖 어디에 쓰여도 안 쪼개진다).
2. **사례번호를 독립 열로 두지 않는다** — 열 하나가 폭을 먹어 정작 보러 온 Hand·Tool 이 화면
   밖으로 밀렸다. 번호는 메타데이터이므로 식별 칸 안에 작게 둔다. 이것만으로 390px 에서
   **배수표 다섯 중 둘이 화면에 완전히 들어왔다**(그전에는 전부 넘쳤다).
3. **대조표는 내용 폭으로 둔다**(`.table-fit`) — 전폭이면 남는 폭이 열에 나뉘어 Hand 열이
   내용 75px 인데 **253px** 로 벌어졌다. 두 값이 멀어지면 대조가 나빠진다.
4. **가로로 스크롤하는 표는 첫 칸을 고정한다**(`.pin-first`) — 이 규칙을 게이트로 세우자
   **손대지 않은 `/beta/` 에서 같은 결함을 찾아냈다.**
5. ★★ **머리글에는 `nowrap` 을 걸지 않는다**(2026-09-18, 비방사능 보고서의 147행 표에서
   `check-render` 가 잡았다 — 768px 칸에 **835px**, 67px 초과). 원인이 데이터가 아니라
   **머리글**이었다: 「Excitation [keV]」가 **150px** 인데 그 열의 값은 「661.659」뿐이다.
   일곱 머리글이 전부 한 줄로 버티며 표를 밀어냈다.
   · `nowrap` 이 지키려던 것은 `3.21202×10¹²` 같은 **공백 없는 값**이고 그것은 **데이터 칸에만**
     있다. 머리글에는 공백이 있으니 **접히는 것이 맞다** — 단위는 `<br />` 로 둘째 줄에 둔다.
   · 이것으로 넘침이 **67 → 0** 이 됐고 쪼개진 값은 **0** 그대로다.
   · ★ 규칙 1 과 **같은 교훈의 세 번째 사례**다(이웃 레포의 「Quantity」 열까지 치면 네 번째):
     **끄는 규칙을 더 세게 걸지 말고 켜는 규칙의 범위를 줄인다.**
   · ★ **다른 표의 머리글 nowrap 은 일괄로 걷지 않았다** — 지금 넘치지 않으므로 실측 근거가
     없고, 근거 없이 건드리면 멀쩡한 조판이 바뀐다. **표가 넘칠 때 첫 번째로 의심할 자리**로
     적어 두고, 실제로 넘치는 표에서만 뗀다. 지키는 것은 `check-render` 의 스크롤넘침이다.

★ `.pin-first` 를 고치며 밟은 것 둘:
· **고정 칸의 바탕은 자리마다 다르다** — `--c-paper` 를 글자로 박아 두어 `.card`(surface) 안에서
  색이 어긋났다. 지금은 `--pin-bg` 변수 하나로 받고 `.card.pin-first` 가 그것을 덮어쓴다.
· ★★ **고정 규칙의 특이도가 줄의 표식을 먹는다** — `/beta/` 에서 **지금 고른 흡수체 줄의 첫 칸이
  다른 줄과 완전히 같은 색**이었다(실측 `rgb(252,252,250)`). 어느 줄을 골랐는지 화면에서 사라진
  것인데 **빌드도 통과하고 클래스도 그대로**라 재 봐야만 보인다. 줄이 `.is-marked` 를 달면
  고정 칸이 그 표식을 따라온다. 게이트는 **클래스 이름을 묻지 않는다** — 이름을 물으면
  「표식 클래스를 안 붙인 바로 그 사고」를 못 본다.
★ 새 도구를 평가할 때: `src/validation/<tool>.cases.ts` + `.test.ts` + `src/pages/validation/<tool>.astro`
를 같은 꼴로 만들고 `src/validation/registry.ts` 의 `status` 를 `"planned"` → `"published"` 로 올린다.
