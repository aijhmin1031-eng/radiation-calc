/** 이 배포본이 **실제로 들고 있는 기능**. 빌드 시점에 정해진다.
 *
 *  ★★ 계기(2026-09-14 소유주 지적 「saved 이거 제대로 작동해??? 저장버튼이 없는데??」).
 *    환경변수가 없으면 `SaveBar` 가 조용히 `return null` 했는데, **머리글의 `Saved` 칸과
 *    `/saved/` 낱장은 그대로 남아** 「저장하면 RC-GAM-20260914-0001 같은 참조번호가 붙는다」고
 *    설명하고 있었다. 찾아온 사람은 그 글을 읽고 계산기로 갔다가 **저장 단추를 못 찾는다.**
 *    라이브 실측: 도구 쪽 저장 조작부 **0개** · 머리글 `Saved` 칸 **있음**.
 *
 *  ★ **없는 기능을 광고하지 않는다.** 조용히 사라지는 것과 없는 척하지 않는 것은 다르다 —
 *    기능은 꺼도 되지만, 꺼 놓고 **켜져 있다고 말하면 안 된다.**
 *    환경변수가 들어오면 내비도 낱장도 저절로 따라 켜진다.
 *
 *  ★ `lib/auth.ts` 가 supabase 라이브러리를 들고 오므로 여기서 **따로** 판정한다 —
 *    Astro 낱장 머리말이 그 무거운 것을 빌드에 끌어들이지 않게.
 */
export const SAVE_ENABLED = Boolean(
  import.meta.env.VITE_SUPABASE_URL && import.meta.env.VITE_SUPABASE_ANON_KEY,
);
